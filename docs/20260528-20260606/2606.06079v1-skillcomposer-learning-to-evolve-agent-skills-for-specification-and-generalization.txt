Title: SkillComposer: Learning to Evolve Agent Skills for Specification and Generalization

URL Source: https://arxiv.org/pdf/2606.06079v1

Published Time: Fri, 05 Jun 2026 01:06:27 GMT

Number of Pages: 15

Markdown Content:
## SkillComposer: Learning to Evolve Agent Skills for Specification and Generalization 

Qi Zhang 1,2 *† , Zhaopeng Feng 2,3 *, Xiaonan Shi 2, Xiaomeng Hu 1,Chu Liu 2, Pengjun Xie 2, Xiaobin Wang 2‡, Jieping Ye 2,

Bryan Hooi 3, Haobo Wang 1, Junbo Zhao 1‡1Zhejiang University, 2Tongyi Lab, 3National University of Singapore 

Abstract 

Agent skills, which consist of reusable strate-gies that guide agent reasoning and action, have shown strong potential for improving model ca-pability at inference time. However, current skill construction methods treat the problem as one-shot extraction, overlooking a fundamental tension: a skill tailored to the specific task fails to transfer, while the abstracted skill often pro-vides insufficient guidance. We attribute this fragility to the absence of explicit mechanisms for skill specification and generalization . To address this gap, we introduce SkillComposer, a framework that decomposes skill construction into three learnable operations: create , improve ,and merge . Trained via systematic rejection sampling recipe, SkillComposer enables lan-guage models to self-evolve skills at inference time and supports three deployment modes: offline for building generalized libraries, on-line for task-specific refinement, and hybrid for combining both. Comprehensive experiments on τ 2-Bench, LiveCodeBench v6, and App-World show that SkillComposer consistently outperforms baselines. Our SkillComposer-4B improves a 27B executor by up to +4.5 on agent tasks and +3.4 on code tasks, while general-izing across domains and task types unseen during training. Analysis reveals that merge 

and improve address orthogonal quality dimen-sions and that skill composition is a transfer-able meta-ability, providing a practical recipe for skill-augmented inference. 

1 Introduction 

Large language models (LLMs) have demonstrated remarkable progress across a wide range of tasks, yet they still struggle with novel scenarios that de-mand structured problem-solving strategies (Xu and Yan, 2026). To bridge this gap, recent work has 

> *

Equal contribution. 

> †

Correspondence to: cheung_se@zju.edu.cn ,

xuanjie.wxb@alibaba.com 

> ‡

Corresponding authors. Task-Trajectory Pairs         

> Offline : From Specification to Generalization
> Online /Hybrid : From Generalization to Specification
> Non-Skill Inference
> Source Tasks
> Raw Skills
> Skill Create
> Iterative Skill Merge
> Target Task
> Initial Skill
> Iterative Skill Improve
> Non-Skill Trajectory
> Online Initialization: Skill Create
> Evolved Skill
> Skill Lib
> Hybrid Initialization: Retrieve from Skill Lib

Figure 1: Three deployment modes of SkillComposer. 

Offline builds a generalized skill library via creation and merging; Online creates and iteratively refines skills per task; Hybrid initializes from an offline library and specializes via online evolution. 

introduced agent skills : reusable natural-language instructions, each comprising a name, a trigger con-dition, and a procedural body, that encode domain-specific procedures in a modular format (Anthropic, 2025; Wang et al., 2025). By loading relevant skills into the context, models can leverage accumulated experience without parameter updates, offering a lightweight and interpretable mechanism for capa-bility enhancement (Liang et al., 2026). Despite their promise, current skill-based ap-proaches face several fundamental limitations: they rely on high-quality human-written skills (Li et al., 2026) or on extracting procedural knowledge from successful trajectories (Fang et al., 2026), both of which are costly and difficult to scale. When mod-els generate skills autonomously, the results often yield limited or even negative performance gains, as the models lack mechanisms for quality con-trol, refinement, and abstraction (Ni et al., 2026; Ling et al., 2026). More critically, effective skills require both generalization (abstracting across sim-1

> arXiv:2606.06079v1 [cs.CL] 4 Jun 2026

ilar tasks to produce transferable strategies) and 

specification (tailoring skills to particular task pat-terns), yet existing methods provide no systematic way to achieve either. A skill that is too specific fails to transfer to other relevant tasks, while one that is too general provides insufficient guidance for other particular tasks (Ling et al., 2026; agentskills, 2025). In this work, we observe that skill construc-tion can be decomposed into three learnable abil-ities: (1) Skill Create extracts reusable procedu-ral knowledge from raw execution trajectories, (2) 

Skill Merge drives generalization by consolidating semantically similar skill pairs into broader, more transferable ones, and (3) Skill Improve drives 

specification by refining a skill to better capture task-specific patterns based on new execution ex-perience. Together, these operations define a com-plete lifecycle for skill evolution: from initial ex-traction, through cross-task abstraction to iterative refinement. We propose SkillComposer , which enables language models to self-evolve its skills at inference time without ground-truth supervision .Moreover, we use delta pass rate guided rejection sampling to synthesize supervised fine-tuning data to further enhance these three abilities. Building on these abilities, SkillComposer sup-ports three modes of operation that address differ-ent practical scenarios (Figure 1). In Offline mode, the model builds a general-purpose skill library from a training set via iterative creation and merg-ing, and retrieves relevant skills for new tasks at inference time. In Online mode, the model starts with an empty library and iteratively creates and refines skills on the fly, driving specification in real time. This mode requires no prior data and is appli-cable to open-ended or streaming scenarios. Hybrid 

mode combines both by initializing with a general-ized offline library and then specializing it through online evolution, balancing broad coverage with task-specific adaptation. We evaluate SkillCom-poser on code generation (LiveCodeBench v6) and agent tasks ( τ 2-Bench, AppWorld), demonstrat-ing consistent improvements across models and domains. Our contributions are as follows: • We identify specification and generalization as two orthogonal dimensions of skill qual-ity, and decomposing skill construction into creating, improving, and merging provides an explicit mechanism for this trade-off. • We propose SkillComposer, a rejection-sampling-based framework that teaches lan-guage models to compose, refine, and abstract skills, supporting offline, online, and hybrid deployment modes. • Comprehensive experiments across multi-ple benchmarks, model scales, and both in-domain and out-of-distribution settings demonstrate the effectiveness and general-ization of SkillComposer. Analysis further reveals that Skill Merge and Improve ad-dress complementary quality dimensions and that skill composition transfers across task types, providing a practical recipe for skill-augmented inference. 

2 Related Work 

Agent skills and skill libraries. Agent skills have recently emerged as a practical abstraction for ex-tending LLM agents at inference time. Unlike a single tool call, a skill package reusable pro-cedural knowledge, trigger conditions, and some-times executable resources that can be loaded on demand (Anthropic, 2025; Xu and Yan, 2026). Re-cent work has therefore studied how to build, or-ganize, and reuse skill libraries. SkillX constructs a plug-and-play skill knowledge base by distill-ing trajectories into hierarchical skills and refining them with execution feedback (Wang et al., 2026). SkillRL distills past experience into a hierarchical SkillBank and lets the skill library co-evolve with an agent policy during reinforcement learning (Xia et al., 2026). Complementary work upgrades tex-tual skills into executable program functions that in-tervene in the agent loop (Liu et al., 2026a). These works show that skills can serve as compact ex-perience carriers for LLM agents. However, they mainly treat skills as external artifacts to be col-lected, retrieved, or optimized, while the ability to construct high-quality skills remains dependent on human authoring, successful trajectories, or exter-nal optimization loops. 

Skill evolution and quality control. Closest to our work are recent methods that move from static skill reuse to automatic skill evolution. EvoSkill discov-ers and edits skills through iterative failure analysis and validation (Alzubi et al., 2026); CoEvoSkills couples a skill generator with a surrogate verifier to build multi-file skill packages without ground-truth test content (Zhang et al., 2026); and SkillClaw ag-gregates cross-user interaction traces to collectively refine a shared skill repository (Ma et al., 2026). 2Source Skill       

> Semantically Similar Skill
> Generalized Skill
> Specified Skill
> Target Task Source Task
> Raw Trajectory Skill-Guided
> Trajectory Executor
> Skill
> Improve
> Skill
> Merge
> Skill
> Create
> Task Trajectory Skill Detail Specification Generalization Specification Skill Lifecycle

Figure 2: The lifecycle of a skill under SkillComposer. A skill is first created from a raw execution trajectory (creation ). Multiple semantically similar while task-specific skills are then consolidated via Skill Merge to form broader, more transferable skills ( specification → generalization ). When applied to a new task, a generalized skill is iteratively refined via Skill Improve to better fit the target task ( generalization → specification ). The resulting specialized skill is finally consumed by the skill executor to guide inference. 

Meanwhile, benchmark studies reveal that skill ben-efits are fragile: curated skills can substantially im-prove agents, but self-generated skills may provide no average gain (Li et al., 2026), and skill utility degrades under realistic retrieval and adaptation settings (Liu et al., 2026b). These findings suggest that the core bottleneck is not merely having more skills, but constructing skills with explicit qual-ity control, task-level specification, and cross-task generalization. Different from prior work, Skill-Composer decomposes skill construction into three trainable abilities– create , improve , and merge –and learns them through rejection-sampled supervision filtered by delta pass rate. In this way, skill evolu-tion becomes an intrinsic capability of the language model itself: Skill Improve drives specification, Skill Merge drives generalization, and the result-ing model can self-evolve skill libraries in offline, online, and hybrid settings. 

3 Methodology 

In this section, we dive into the framework of Skill-Composer. We first formalize three core skill oper-ations (§3.2), then describe how they compose into three inference-time application scenarios (§3.3), and finally introduce a rejection-sampling-based training procedure that strengthens these operations (§3.4). Figure 2 provides an overview of the lifecy-cle of a skill under our proposed SkillComposer. 

3.1 Preliminary 

We begin by formalizing the concept of agent skills. In the most general form, agent skills comprise a markdown-style document SKILL.md along with optional auxiliary files such as references, scripts, and assets (Anthropic, 2025). In this work, we fo-cus on the main body of agent skills, where each skill s is defined as a tuple of three components: a name n, a description d, and a body b. The name serves as an identifier, the description specifies the trigger condition under which the skill should be ac-tivated, and the body encodes the procedural strat-egy to be followed. 

3.2 Skill Operations Formulation 

Maintaining a high-quality skill library requires addressing three fundamental challenges: i) how to extract a reusable skill from a raw trajectory produced without any skill guidance, ii ) how to pre-vent the unbounded growth of a skill library while promoting generalization, and iii ) how to refine an existing skill based on new execution experi-ence. We address these challenges by introducing three composable operations: create , merge , and 

improve .

Skill Create. When the agent solves a task without leveraging any prior skill, the resulting trajectory contains implicit procedural knowledge that can benefit future tasks with a similar structure. Skill Create distills such a trajectory into a reusable skill: 

s = SkillComposer create (x, LLM( x)) 

Skill Merge. As skills accumulate, the library risks unbounded growth and over-specification, where many narrowly-scoped skills overlap in functional-ity. To mitigate this, Skill Merge consolidates two semantically similar skills, s1 and s2, into a single, more general one: 

s = SkillComposer merge (s1, s 2)

Specifically, to measure the semantical similarity of two skills, we introduce a multi-view similarity mechanism where we compute similarity across every dimension for each skill pair: 

sim( s1, s 2) = 13

X

> I∈{ n,d,b }

sim( I1, I 2) (1) 3Only pairs with similarity larger than a threshold δ

will be selected to conduct Skill Merge. 

Skill Improve. When the agent solves a task under the guidance of an existing skill so, the resulting trajectory may reveal new patterns or refinements that the original skill does not capture. Skill Im-prove incorporates these insights to produce a more effective skill: 

s = SkillComposer improve (x, s o, LLM( x, s o)) 

3.3 Inference-Time Skill Evolution 

The three operations can be chained in different orders depending on whether a pre-existing task set is available and whether task-specific refinement is desired at inference time. We organize these compositions into three deployment modes. Algo-rithm 1 also unifies the three modes into a single procedure: 

Offline. The most straightforward way to leverage skills at inference time is to pre-build a skill library from a given task set. In offline mode, we first generate raw trajectories for all tasks and apply Skill Create to extract one skill per trajectory. We then compute pairwise similarities using Equation 1 and iteratively merge the most similar pairs until no pair exceeds δ. The resulting library captures generalized procedural knowledge. At inference time, we retrieve the top-k skills for each target task and employ a self-select mechanism that allows the executor to choose the most appropriate skill, or abstain if none is relevant. 

Online. In scenarios where no pre-existing task set is available, we operate in online mode. For each incoming task, the agent generates a raw trajectory and applies Skill Create to initialize a skill. The skill is then iteratively refined: at each iteration, the agent re-executes the task under the current skill’s guidance, and Skill Improve updates the skill based on the new trajectory. This process repeats for a fixed number of iterations T , progressively driving specification. 

Hybrid. The hybrid mode combines the strengths of both approaches. It initializes with an offline-constructed skill library, then applies online evo-lution on top. For each new task, we retrieve and self-select a relevant skill from the library, then re-fine it through iterative improvement. This enables the model to start from a generalized foundation and progressively specialize toward the target task. 

3.4 Training via Rejection Sampling 

While any language model can perform the three operations zero-shot, the output quality varies con-siderably. We strengthen them via supervised fine-tuning on rejection-sampled data (Yuan et al., 2023), using a unified quality signal: delta pass rate , the performance change caused by a candidate skill. A training example is accepted only when the candidate improves the executor’s pass@ k relative to the appropriate baseline by at least a threshold ϵ.Concretely, we generate n raw trajectories per task and apply the base model to produce candi-date skills. For create , we compare the executor’s pass@ 1 with and without the candidate skill on the source task and retain the example if the improve-ment exceeds ϵc:

∆create = pass@ 1( LLM (x, s )) 

− pass@ 1( LLM (x)) ≥ ϵc (2) For merge and improve , the criterion generalizes to multiple evaluation tasks. Merge assesses the consolidated skill s against the two original skills 

s1, s 2 on their respective source tasks x1, x 2:

∆merge = 12

> 2

X

> i=1

pass@ 1( LLM (xi, s )) 

− pass@ 1( LLM (xi, s i))  ≥ ϵm (3) Improve evaluates the refined skill against the orig-inal skill so on both the source task x (from which 

so was derived) and a target task x′:

∆improve = 12

X

> xi∈{ x,x ′}

pass@ 1( LLM (xi, s )) 

− pass@ 1( LLM (xi, s o))  ≥ ϵi (4) For improve, we include both cross-task ( x′̸ = x)and same-task ( x′ = x) pairs, so the training sig-nal captures both specification and generalization. Detailed algorithms for all three procedures are provided in Appendix B. 

4 Experiments 

4.1 Experimental Settings Training and Evaluation For coding task, we use OpenCodeReasoning (Ahmad et al., 2025) for both rejection sampling and building the initial skill library. For rejection sampling of the agent task, we follow AutoForge (Cai et al., 2025) to synthe-size datasets similar to the Retail domain in τ 2-Bench. In summary, we synthesize around 7,000 4Algorithm 1 Inference-Time Skill Evolution 

Require: Task set X , skill executor LLM, mode 

∈ {offline, online, hybrid}, max iterations T ,skill library L ← ∅ 

Ensure: Evolved skill library L 

> 1:

Stage 1: Skill Library Initialization  

> 2:

if mode ∈ {offline, hybrid} then  

> 3:

for each task x ∈ X do  

> 4:

τ ← LLM (x) ▷ Raw trajectory  

> 5:

s ← SkillComposer create (x, τ ) 

> 6:

L ← L ∪ { s} 

> 7:

end for  

> 8:

repeat  

> 9:

(si, s j ) ← RetrieveSimilarPair (L, δ ) 

> 10:

s ← SkillComposer merge (si, s j ) 

> 11:

L ← (L \ { si, s j }) ∪ { s} 

> 12:

until no similar pairs remain  

> 13:

end if  

> 14:

Stage 2: Skill Evolution  

> 15:

for each target task x′ do  

> 16:

if mode = offline then  

> 17:

s ← SelfSelect (Retrieve (L, x ′, k ))  

> 18:

τ ← LLM (x′, s ) 

> 19:

else  

> 20:

if mode = online then  

> 21:

τ ← LLM (x′) 

> 22:

s ← SkillComposer create (x′, τ ) 

> 23:

else if mode = hybrid then  

> 24:

s ← SelfSelect (Retrieve (L, x ′, k ))  

> 25:

end if  

> 26:

for t = 1 to T do  

> 27:

τ ← LLM (x′, s ) 

> 28:

s ← SkillComposer improve (x′, s, τ ) 

> 29:

end for  

> 30:

end if  

> 31:

L ← L ∪ { s} 

> 32:

end for 

supervised fine-tuning data points. We further train Qwen3.5-4B (Team, 2026) with LlamaFactory, and run skill executor and SkillComposer using vllm. We extend more implementation details in Ap-pendix A. 

Benchmarks We evaluate SkillComposer on three benchmarks spanning agent and code tasks. (1) τ 2-Bench (Barres et al., 2025) is a multi-turn agent benchmark that simulates realistic customer service interactions across three domains: Retail, Airline, and Telecom. Each domain requires the agent to follow domain-specific policies and exe-cute tool calls to resolve user requests. Since the training data only consists of the Retail domain, evaluation on Airline and Telecom can be regarded as a cross-domain scenario. (2) LiveCodeBench v6 (Jain et al., 2024) is a contamination-free code generation benchmark comprising problems of varying difficulty (Easy, Medium, Hard) sourced from recent programming contests. (3) App-World (Trivedi et al., 2024) is an interactive agent benchmark that requires autonomous app usage through API calls across diverse everyday applica-tions. We use AppWorld as an out-of-distribution evaluation to assess cross-task generalization, as it is entirely unseen during training. 

4.2 Main Results 

Table 1 presents results on τ 2-Bench and Live-CodeBench v6. We compare against No Skill 

(vanilla LLM) and MemP (Fang et al., 2026) (pro-cedural memory extracted from valid trajectories), and additionally report results for the untrained Qwen3.5-4B to isolate the contribution of rejection sampling SFT. All training data is collected using the 4B model. For offline skill library construc-tion, we use the training set of τ 2-Bench for agent tasks and 500 randomly sampled instances from OpenCoderReasoning for code tasks. 

Effectiveness of SkillComposer. On the 4B ex-ecutor, SkillComposer consistently outperforms both baselines and its untrained counterparts across all modes. The hybrid mode achieves the best τ 2-Bench score (85.7, +6.2 over No Skill), while the online mode leads on LiveCodeBench (59.1, +2.5). MemP degrades LiveCodeBench performance to 47.4, well below the No Skill baseline of 56.6, un-derscoring the risk of uncontrolled skill extraction. In contrast, SkillComposer online improves over its untrained counterpart by 2.0 points on τ 2-Bench and 2.4 points on LiveCodeBench, confirming that rejection-sampling-based training yields meaning-ful quality gains. We provide detailed performance curves across iterations in Appendix C (Figure 5). Moreover, we further provide inference effi-ciency comparison across the base model and our trained SkillComposer-4B. As shown in table 6, as skills evolve, the average token consumption of both methods decreases while the average perfor-mance keep increasing, which indicates the steady performance and efficiency of our methods. No-tably, except for the performance superiority, our trained SkillComposer-4B also consistently show 5Method Tau2Bench LiveCodeBench v6 Retail Airline Telecom Overall Easy Medium Hard Overall 

Skill Executor: Qwen3.5-4B 

No Skill 71.3 68.8 93.1 79.5 99.1 67.3 26.8 56.6 MemP 72.5 77.5 93.8 82.0 100.0 42.3 22.5 47.4 Qwen3.5-4B offline 72.5 76.3 92.5 81.3 97.7 61.5 25.0 53.7 online 70.6 80.0 96.3 82.8 98.1 68.9 26.5 56.7 hybrid 73.8 77.5 93.8 82.5 95.4 64.1 29.6 56.0 SkillComposer-4B offline 72.5 75.0 92.5 81.0 95.4 65.4 25.8 54.7 online 74.4 78.8 98.1 84.8 98.1 72.7 29.3 59.1 

hybrid 77.5 82.5 95.6 85.7 98.5 69.2 31.3 59.0 

Skill Executor: Qwen3.5-27B 

No Skill 75.6 77.5 94.4 83.5 99.5 90.4 62.0 79.7 MemP 71.3 78.8 97.5 83.3 100.0 67.3 47.5 66.3 Qwen3.5-4B offline 72.5 73.8 96.3 82.3 100.0 84.6 55.0 74.9 online 77.5 78.8 98.8 86.3 100.0 91.9 64.5 81.4 hybrid 77.5 80.0 97.5 86.0 99.2 90.4 64.2 80.6 SkillComposer-4B offline 73.8 73.8 97.5 83.3 98.3 89.4 59.7 78.0 online 79.4 83.5 98.8 88.0 100.0 94.2 66.8 83.1 

hybrid 78.1 81.3 98.8 87.0 99.4 93.3 67.2 82.9         

> Table 1: Main results on τ2-Bench (agent) and LiveCodeBench v6 (code). Rejection sampling and offline library construction use Qwen3.5-4B as the skill executor, with the τ2-Bench training set and randomly sampled 500 OpenCoderReasoning samples as data sources. Bold denotes the best and italic the second best within each executor.

better efficiency. 

Comparison across modes. Online and hybrid consistently outperform offline, indicating that it-erative skill refinement at inference time provides substantial gains beyond static retrieval. Hybrid achieves the strongest τ 2-Bench results by com-bining a generalized library with online specializa-tion. Notably, the performance gains of offline and hybrid modes are more pronounced on τ 2-Bench than on LiveCodeBench. We attribute this to the offline library construction: the τ 2-Bench library is built from its in-distribution training set, while the code library draws from only 500 randomly sampled OpenCoderReasoning instances, which offers limited coverage of LiveCodeBench’s task distribution. In contrast, the online mode, which requires no pre-built library and instead refines skills per task, excels on LiveCodeBench, where task-specific specification is most beneficial. Furthermore, on Hard-level code problems, hybrid consistently out-performs online (31.3 vs. 29.3 on 4B; 67.2 vs. 66.8 on 27B), suggesting that an initial skill library serves as valuable prior experience that particularly benefits the executor on more challenging tasks where structured guidance is most needed. How-ever, on the Simple and Medium-Level problems, hybrid lags behind the online setting, which in-dicates that unnecessary skills might mislead the language models when facing simpler tasks. 

4.3 Cross-Model Generalization 

Since all training data is collected with Qwen3.5-4B as the skill executor, the 27B results directly evaluate cross-model transfer. As shown in Ta-ble 1, SkillComposer generalizes effectively across model scales: SkillComposer-4B-online achieves 88.0 on τ 2-Bench ( +4.5 over No Skill) and 83.1 on LiveCodeBench ( +3.4), while the hybrid mode reaches 67.2 on Hard-level code problems. These results confirm that skills composed by a smaller model remain effective when consumed by a sub-stantially larger executor, suggesting that skill qual-ity is largely decoupled from the composer’s capac-ity. 64.4 Cross-Domain Generalization 

Our τ 2-Bench training data is sourced exclusively from the Retail domain, making Airline and Tele-com results a direct test of cross-domain general-ization. On the 4B executor, SkillComposer-4B-hybrid achieves 82.5 on Airline ( +13.7 over No Skill) and 95.6 on Telecom ( +2.5). On the 27B ex-ecutor, SkillComposer-4B-online reaches 83.5 on Airline ( +6.0) and 98.8 on Telecom ( +4.4). The Airline gains suggest that skills learned from Retail, which shares structural similarities as a customer-service domain, transfer effectively to related but unseen domains. This indicates that SkillComposer acquires generalizable composition abilities rather than domain-specific patterns. 

5 Ablation and Analysis 

We now investigate the mechanisms behind Skill-Composer’s gains. We first ablate the individual skill operations to verify that merge and improve serve complementary roles (§5.1). We then ask whether the learned composition ability is tied to the training domain or transfers across task types (§5.2), whether it generalizes to entirely unseen benchmarks (§5.3), and whether iterative skill evo-lution offers genuine advantages over brute-force repeated sampling (§5.4). 

5.1 Ablation on Skill Operations 

We ablate the skill operations used during offline library construction to isolate each operation’s con-tribution. Table 2 reports offline-mode results with Qwen3.5-4B as the skill executor. 

Skill Operations τ 2-Bench LiveCodeBench v6 

Create 73.8 58.5 

Create/Improve 75.6 57.3 Create/Merge 77.5 59.0 

Create/Merge/Improve 76.9 57.9 

Table 2: Ablation on skill operations used during of-fline library construction (Qwen3.5-4B executor). Skill Merge provides the largest gain by promoting gener-alization, while Skill Improve is less beneficial in the offline setting, where transferability is prioritized. 

Skill Merge is the most impactful operation: adding it to Skill Create yields +3.7 on τ 2-Bench and +0.5 on LiveCodeBench. This aligns with our framework’s design, as offline libraries prioritize generalized skills, and Skill Merge is precisely the operation that drives generalization. To visualize 2 4 6 8     

> t-SNE 1
> 2
> 1
> 0
> 1
> 2
> 3
> 4
> 5
> t-SNE 2
> Before Skill Merge
> 4.5 4.0 3.5 3.0 2.5 2.0
> t-SNE 1
> 0.0
> 0.5
> 1.0
> 1.5
> 2.0
> t-SNE 2
> After Skill Merge

Figure 3: t-SNE visualization of the Retail skill library before (left) and after (right) Skill Merge. Merging consolidates semantically similar skills, producing a 

sparser and more general library with reduced redun-dancy. 

this effect, Figure 3 shows t-SNE embeddings of the Retail skill library before and after merging. After Skill Merge, the skill distribution becomes notably sparser, confirming that merge effectively consolidates redundant skills into fewer, more gen-eral ones. Adding Skill Improve alone to Create provides moderate gains on τ 2-Bench ( +1.8) but slightly hurts LiveCodeBench ( −1.2), suggesting that specification can reduce transferability for un-seen tasks. Combining all three operations per-forms slightly below Skill Create/Merge, indicating that indiscriminate specification during offline con-struction partially counteracts the generalization benefit of Skill Merge. These results reinforce that Skill Improve is better suited for online and hybrid settings, where skills are refined toward specific target tasks. 

5.2 Transferability of Skill Composition 

We investigate whether the skill composition ability generalizes across task types or remains specific to the training scenario. We train SkillComposer with agent-only, code-only, and mixed (agent & code) rejection sampling data, evaluating all variants on LiveCodeBench v6 in online mode with Qwen3.5-27B as the skill executor (Table 3). SkillComposer Easy Medium Hard Overall Qwen3.5-4B 100.0 91.9 64.5 81.4 +agent 100.0 93.5 66.8 82.9 +code 99.5 95.8 68.0 84.0 

+agent & code 100.0 94.2 66.8 83.1 

Table 3: Transferability of skill composition across task types, evaluated on LiveCodeBench v6 (online mode, Qwen3.5-27B executor). Agent-only training still im-proves code performance, confirming that skill compo-sition is a general-purpose ability. 

Training with agent-only data improves over-7all performance from 81.4 to 82.9 ( +1.5), despite never encountering code tasks during training. This confirms that skill composition is a general-purpose ability: structuring procedural knowledge and iter-ative refinement transfer across task types. Code-only training yields the strongest results (84.0, 

+2.6) due to direct domain alignment, while the mixed setting used in practice achieves 83.1, offer-ing a balanced trade-off. 

5.3 Cross-Task Generalization 

To further evaluate whether SkillComposer gener-alizes to entirely unseen agent tasks, we test the online mode on AppWorld (Trivedi et al., 2024), a benchmark not used during training or rejection sampling. We compare the vanilla model (No Skill), the untrained Qwen3.5-4B composer, and SkillComposer-4B, all using Qwen3.5-4B as the skill executor. Results are shown in Table 4. SkillComposer-4B achieves the best perfor-mance on both metrics, improving accuracy to 57.7 (+2.5 over Vanilla) while maintaining a high pass rate of 88.3. Notably, the untrained composer im-proves accuracy (57.1) but degrades pass rate from 88.1 to 86.0, indicating that naively generated skills can introduce errors that reduce task completion re-liability. In contrast, SkillComposer improves both metrics simultaneously, confirming that rejection-sampling-based training produces skills that en-hance correctness without sacrificing robustness. This result demonstrates that the skill composition ability learned from τ 2-Bench transfers effectively to a structurally different agent benchmark. Method Vanilla Qwen3.5-4B SkillComposer-4B Accuracy 55.2 57.1 57.7    

> Pass Rate 88.1 86.0 88.3

Table 4: Cross-task generalization on AppWorld (online mode, Qwen3.5-4B executor). SkillComposer improves both accuracy and pass rate, while the untrained com-poser degrades pass rate. 

5.4 Iterative Evolution vs. Repeated Sampling 

A natural question is whether iterative skill evo-lution offers advantages beyond simply sampling more attempts. To answer this, we compare two strategies under the same inference budget of k

rounds: i) vanilla pass@ k, which independently samples k solutions without any skill and selects the best one, and ii ) SkillComposer online evolu-tion, which creates a skill from the first trajectory Iter 1 Iter 2 Iter 3 Iter 4 Iter 5      

> 55
> 60
> 65
> 70
> 75
> Pass@k (  )
> 73.71
> (a) Skill Executor: Qwen3.5-4B
> Iter 1 Iter 2 Iter 3 Iter 4 Iter 5
> 78
> 80
> 82
> 84
> 86
> 88
> 90
> 92
> 94
> Pass@k (  )
> 91.43
> (b) Skill Executor: Qwen3.5-27B
> Vanilla SkillComposer (Ours)

Figure 4: Pass@ k vs. SkillComposer online evolution over k rounds on LiveCodeBench v6. Both strategies share the same starting point at k=1 . Iterative skill re-finement consistently outperforms independent repeated sampling, with the gap widening as k increases. 

and iteratively improves it across k rounds. As shown in Figure 4, both strategies start from the same baseline at k=1 , but SkillComposer con-sistently outperforms vanilla pass@ k as the number of rounds increases. On the 4B executor, SkillCom-poser reaches 73.71 at k=5 compared to 69.71 for vanilla pass@ k, a gain of 4.0 points. On the 27B ex-ecutor, the gap persists at 91.43 vs. 88.57 ( +2.86). Notably, the performance gap widens with each ad-ditional round, indicating that iterative skill refine-ment compounds its advantage over independent sampling. This demonstrates that structured skill evolution is a more effective use of additional in-ference budget than brute-force repeated sampling. 

6 Conclusion 

We presented SkillComposer, a framework that de-composes skill construction into three learnable operations: create, improve, and merge. By train-ing these operations via rejection sampling filtered by delta pass rate, SkillComposer enables language models to self-evolve skill libraries at inference time without ground-truth supervision. The frame-work supports three complementary modes: offline for building generalized libraries, online for task-specific refinement, and hybrid for combining both. Experiments on τ 2-Bench, LiveCodeBench v6, and AppWorld demonstrate that SkillComposer consis-8tently outperforms baselines and generalizes across model scales, domains, and task types. Our analy-sis further reveals that merge drives generalization while improve drives specification, and that skill composition is a transferable ability that benefits from but does not require in-domain training data. 

Limitations 

The rejection sampling procedure requires multiple inference passes per task to compute delta pass@ k,making training data collection computationally expensive. Additionally, our experiments are con-ducted with a limited number of model families (Qwen3.5-4B and Qwen3.5-27B); evaluating Skill-Composer across a broader range of architectures and scales would strengthen the generality of our findings. 

References 

agentskills. 2025. agentskills. https://github.com/ agentskills/agentskills . GitHub repository. Wasi Uddin Ahmad, Sean Narenthiran, Somshubra Ma-jumdar, Aleksander Ficek, Siddhartha Jain, Jocelyn Huang, Vahid Noroozi, and Boris Ginsburg. 2025. Opencodereasoning: Advancing data distillation for competitive coding. Salaheddin Alzubi, Noah Provenzano, Jaydon Bing-ham, Weiyuan Chen, and Tu Vu. 2026. Evoskill: Automated skill discovery for multi-agent systems. 

Preprint , arXiv:2603.02766. Anthropic. 2025. skills. https://github.com/ anthropics/skills . GitHub repository. Victor Barres, Honghua Dong, Soham Ray, Xujie Si, and Karthik Narasimhan. 2025. τ 2-bench: Evaluat-ing conversational agents in a dual-control environ-ment. Preprint , arXiv:2506.07982. Shihao Cai, Runnan Fang, Jialong Wu, Baixuan Li, Xinyu Wang, Yong Jiang, Liangcai Su, Liwen Zhang, Wenbiao Yin, Zhen Zhang, and 1 others. 2025. Autoforge: Automated environment synthesis for agentic reinforcement learning. arXiv preprint arXiv:2512.22857 .Runnan Fang, Yuan Liang, Xiaobin Wang, Jialong Wu, Shuofei Qiao, Pengjun Xie, Fei Huang, Huajun Chen, and Ningyu Zhang. 2026. Memp: Exploring agent procedural memory. Preprint , arXiv:2508.06433. Naman Jain, King Han, Alex Gu, Wen-Ding Li, Fanjia Yan, Tianjun Zhang, Sida Wang, Armando Solar-Lezama, Koushik Sen, and Ion Stoica. 2024. Live-codebench: Holistic and contamination free eval-uation of large language models for code. arXiv preprint .Xiangyi Li and 1 others. 2026. Skillsbench: Bench-marking how well agent skills work across diverse tasks. Preprint , arXiv:2602.12670. Yuan Liang, Ruobin Zhong, Haoming Xu, Chen Jiang, Yi Zhong, Runnan Fang, Jia-Chen Gu, Shumin Deng, Yunzhi Yao, Mengru Wang, Shuofei Qiao, Xin Xu, Tongtong Wu, Kun Wang, Yang Liu, Zhen Bi, Jun-gang Lou, Yuchen Eleanor Jiang, Hangcheng Zhu, and 30 others. 2026. Skillnet: Create, evaluate, and connect AI skills. CoRR , abs/2603.04448. George Ling, Shanshan Zhong, and Richard Huang. 2026. Agent skills: A data-driven analysis of claude skills for extending large language model functional-ity. CoRR , abs/2602.08004. Hongjun Liu, Yifei Ming, Shafiq Joty, and Chen Zhao. 2026a. Harnessing llm agents with skill programs. 

Preprint , arXiv:2605.17734. Yujian Liu, Jiabao Ji, Li An, Tommi Jaakkola, Yang Zhang, and Shiyu Chang. 2026b. How well do agen-tic skills work in the wild: Benchmarking llm skill us-age in realistic settings. Preprint , arXiv:2604.04323. Ziyu Ma, Shidong Yang, Yuxiang Ji, Xucong Wang, Yong Wang, Yiming Hu, Tongwen Huang, and Xiangxiang Chu. 2026. Skillclaw: Let skills evolve collectively with agentic evolver. Preprint ,arXiv:2604.08377. Jingwei Ni, Yihao Liu, Xinpeng Liu, Yutao Sun, Mengyu Zhou, Pengyu Cheng, Dexin Wang, Er-chao Zhao, Xiaoxi Jiang, and Guanjun Jiang. 2026. Trace2skill: Distill trajectory-local lessons into trans-ferable agent skills. CoRR , abs/2603.25158. Qwen Team. 2026. Qwen3.5: Accelerating productivity with native multimodal agents. Harsh Trivedi, Tushar Khot, Mareike Hartmann, Ruskin Manku, Vinty Dong, Edward Li, Shashank Gupta, Ashish Sabharwal, and Niranjan Balasubramanian. 2024. AppWorld: A controllable world of apps and people for benchmarking interactive coding agents. In Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers) , pages 16022–16076, Bangkok, Thai-land. Association for Computational Linguistics. Chenxi Wang, Zhuoyun Yu, Xin Xie, Wuguannan Yao, Runnan Fang, Shuofei Qiao, Kexin Cao, Guozhou Zheng, Xiang Qi, Peng Zhang, and Shumin Deng. 2026. Skillx: Automatically constructing skill knowl-edge bases for agents. Preprint , arXiv:2604.04804. Zora Zhiruo Wang, Apurva Gandhi, Graham Neubig, and Daniel Fried. 2025. Inducing programmatic skills for agentic tasks. CoRR , abs/2504.06821. Peng Xia, Jianwen Chen, Hanyang Wang, Jiaqi Liu, Kaide Zeng, Yu Wang, Siwei Han, Yiyang Zhou, Xujiang Zhao, Haifeng Chen, Zeyu Zheng, Cihang Xie, and Huaxiu Yao. 2026. Skillrl: Evolving agents via recursive skill-augmented reinforcement learning. 

Preprint , arXiv:2602.08234. 

9Renjun Xu and Yang Yan. 2026. Agent skills for large language models: Architecture, acquisition, security, and the path forward. Preprint , arXiv:2602.12430. Zheng Yuan, Hongyi Yuan, Chengpeng Li, Guanting Dong, Chuanqi Tan, and Chang Zhou. 2023. Scaling relationship on learning mathematical reasoning with large language models. CoRR , abs/2308.01825. Hanrong Zhang, Shicheng Fan, Henry Peng Zou, Yankai Chen, Zhenting Wang, Jiayu Zhou, Chengze Li, Wei-Chieh Huang, Yifei Yao, Kening Zheng, Xue Liu, Xiaoxiao Li, and Philip S. Yu. 2026. Coevoskills: Self-evolving agent skills via co-evolutionary verifi-cation. Preprint , arXiv:2604.01687. Yanzhao Zhang, Mingxin Li, Dingkun Long, Xin Zhang, Huan Lin, Baosong Yang, Pengjun Xie, An Yang, Dayiheng Liu, Junyang Lin, Fei Huang, and Jingren Zhou. 2025. Qwen3 embedding: Advancing text embedding and reranking through foundation models. 

> arXiv preprint arXiv:2506.05176 .

A Implementation Details 

In this paper, we use Qwen3-Embedding-8B (Zhang et al., 2025) as the embedding model to calculate the similarity. We list the global hyperpa-rameter settings across all tasks below: 

Parameter Value 

similarity threshold δ 0.8 delta pass rate for Skill Create ϵc 0.4 delta pass rate for Skill Merge ϵm 0.4 delta pass rate for Skill Improve ϵi 0.4 

> Table 5: Hyperparameters details for rejection sampling.

A.1 Code Rejection Sampling For rejection sampling, we use 8,000 data points sampled from OpenCodeRea-soning, and run 5 separate runs for each data point. 

Skill Library Building For the initial skill li-brary building of offline and hybrid modes, we in-corporate 500 randomly sampled data points from the OpenCodeReasoning dataset. 

A.2 Agent Skill Library Building For the initial skill li-brary building of offline and hybrid modes, we use the training split of τ 2-Bench. 

A.3 Evaluation Details 

For agent tasks, we conduct 4 separate runs across all methods and settings. For code tasks, we con-duct 5 separate runs across all methods and settings. Finally, we report the average score of the best it-eration across all runs for each setting. Moreover, during the skill library building of the evaluation process, we use the same similarity threshold δ as rejection sampling. 

A.4 Training Details 

For supervised fine-tuning, we trained Qwen3.5-4B with LlamaFactory. We list the detailed hyperpa-rameters below: 

B Rejection Sampling Procedures 

We provide detailed pseudo-code for the three re-jection sampling procedures introduced in §3.4. 10 Parameter Value 

Learning Rate 1e-6 Train Batch Size 64 Number of Training Epochs 3Max Length 65536 LR Scheduler cosine Warmup Ratio 0.1 

Table 6: Supervised fine-tuning details for SkillCom-poser. 

Algorithm 2 Rejection Sampling for Skill Create 

Require: Task set X , skill executor LLM, base composer C, samples n, threshold ϵc

Ensure: Training set Dcreate  

> 1:

Dcreate ← ∅  

> 2:

for each task x ∈ X do  

> 3:

pbase ← pass@ 1( LLM (x), n ) 

> 4:

τ ← sample one trajectory from LLM (x) 

> 5:

s ← C create (x, τ ) 

> 6:

pskill ← pass@ 1( LLM (x, s ), n ) 

> 7:

if pskill − pbase ≥ ϵc then  

> 8:

Dcreate ← D create ∪ { (x, τ ) → s} 

> 9:

end if  

> 10:

end for Algorithm 3 Rejection Sampling for Skill Merge 

Require: Skill-task pairs {(si, x i)}, skill executor LLM, base composer C, samples n, threshold 

ϵm

Ensure: Training set Dmerge  

> 1:

Dmerge ← ∅  

> 2:

Compute pairwise similarity among all skills  

> 3:

for each similar pair (s1, x 1), (s2, x 2) do  

> 4:

s ← C merge (s1, s 2) 

> 5:

∆ ← 12

P2

> i=1

[pass@ 1( LLM (xi, s ), n ) −

pass@ 1( LLM (xi, s i), n )]  

> 6:

if ∆ ≥ ϵm then  

> 7:

Dmerge ← D merge ∪ { (s1, s 2) → s} 

> 8:

end if  

> 9:

end for Algorithm 4 Rejection Sampling for Skill Improve 

Require: Skill-task pairs {(si, x i)}, skill executor LLM, base composer C, samples n, threshold 

ϵi

Ensure: Training set Dimprove  

> 1:

Dimprove ← ∅  

> 2:

Compute skill-task similarity matrix  

> 3:

for each selected pair (so, x ′) with original task x do  

> 4:

τ ′ ← sample one trajectory from LLM (x′, s o) 

> 5:

s ← C improve (x′, s o, τ ′) 

> 6:

∆ ← 12

P

> xi∈{ x,x ′}

[pass@ 1( LLM (xi, s ), n )−

pass@ 1( LLM (xi, s o), n )]  

> 7:

if ∆ ≥ ϵi then  

> 8:

Dimprove ← D improve ∪ { (x′, s o, τ ′) → s} 

> 9:

end if  

> 10:

end for 

C Performance by Iteration 

Figure 5 presents the performance trajectory across iterations for both online and hybrid modes. For 

τ 2-Bench, we report the overall score across all three domains (Retail, Airline, and Telecom). The curves illustrate how skill quality evolves over suc-cessive iterations, with SkillComposer consistently achieving steeper improvement curves compared to the untrained model in both settings. 

D Inference Efficiency across Iterations E Prompt Template 

We present the prompt templates used for the four core operations in SkillComposer. Each prompt is instantiated with task-specific variables (denoted by {{variable}} ) at inference time. 

E.1 Skill Create Prompt 

The following prompt instructs the skill composer to extract a reusable skill from a completed task trajectory. Skill Create Prompt                           

> You are aprofessional skill creator. Askill is an executable procedure that provides specialized instructions for specific tasks. [Task Description] 1. Given atask and acorresponding trajectory generated by alanguage model, your task is to generate areusable/generalizable skill based on the

11 Iter 1 Iter 2 Iter 3 Iter 4 Iter 5                              

> 56
> 57
> 58
> 59
> Code Acc. (%)
> Qwen3.5-4B
> (Online)
> Iter 1 Iter 2 Iter 3 Iter 4 Iter 5
> 53
> 54
> 55
> 56
> 57
> 58
> 59
> 60
> Qwen3.5-4B
> (Hybrid)
> Iter 1 Iter 2 Iter 3 Iter 4 Iter 5
> 79
> 80
> 81
> 82
> 83
> Qwen3.5-27B
> (Online)
> Iter 1 Iter 2 Iter 3 Iter 4 Iter 5
> 74
> 76
> 78
> 80
> 82
> 84
> Qwen3.5-27B
> (Hybrid)
> Iter 1 Iter 2 Iter 3 Iter 4 Iter 5
> 77.5
> 78.0
> 78.5
> 79.0
> 79.5
> 80.0
> Agent Acc. (%)
> Iter 1 Iter 2 Iter 3 Iter 4 Iter 5
> 79.5
> 80.0
> 80.5
> 81.0
> 81.5
> 82.0
> 82.5
> Iter 1 Iter 2 Iter 3 Iter 4 Iter 5
> 81.75
> 82.00
> 82.25
> 82.50
> 82.75
> 83.00
> 83.25
> 83.50
> Iter 1 Iter 2 Iter 3 Iter 4 Iter 5
> 80.5
> 81.0
> 81.5
> 82.0
> 82.5
> 83.0
> 83.5
> Qwen3.5-4B SkillComposer-4B

Figure 5: Average performance by iteration for online and hybrid modes on τ 2-Bench (average scores across Retail, Airline, Telecom) and LiveCodeBench v6. SkillComposer exhibits steeper and more sustained improvement across iterations compared to the untrained baselines in both settings. Iter 1 Iter 2 Iter 3 Iter 4 Iter 5 

> Iteration
> 56.5
> 57.0
> 57.5
> 58.0
> 58.5
> 59.0
> Accuracy (  )

Qwen3.5-4B     

> Iter 1 Iter 2 Iter 3 Iter 4 Iter 5
> Iteration
> 80.0
> 80.5
> 81.0
> 81.5
> 82.0
> 82.5
> 83.0
> Accuracy (  )

Qwen3.5-27B    

> 10000
> 11000
> 12000
> 13000
> 14000
> 15000
> 16000
> Avg Tokens
> 15000
> 16000
> 17000
> 18000
> 19000
> 20000
> 21000
> Avg Tokens
> Base Acc. Ours Acc. Qwen3.5-4B Tokens SkillComposer Tokens

Figure 6: The token consumption across iterations of Qwen3.5-4B and SkillComposer-4B. 

given contents. 2. The reusability/generalizability should be both model-level and task-level: - The skill should help current language model not only solve current task but this type of tasks - The skill should help other language models not only solve current task but this type of tasks [Skill Formulation] The generated skill should consist of three components including name, description and body: 1. **name**: The identifier of the skill. Once the name is generated, it will never be further modified. Therefore, make its name as general and abstract as possible, rather than limiting it to the current input problem and trajectory. 2. **description**: When to trigger, what it does. This is the primary triggering mechanism - include both what the skill does AND specific contexts for when to use 

it. All "when to use" info goes here, not in the body. 3. **body**: A document in markdown-style. It should contain the name of the skill and several sections with detailed instructions (i.e., what you want the skill to do and roughly how it should do it) to solve this type of tasks. [Skill Create Instructions] 1. When writing a skill, try to explain to the model why things are important in lieu of heavy-handed musty MUSTs. 2. Use theory of mind and try to make the skill general and not super-narrow to specific examples. 3. Start by writing a draft and then look at it with fresh eyes and improve it. 4. Make sure not to introduce any specific concepts, examples or details from the input. [Input Details] Here are the task and the trajectory: 

12 ### Task {{task}} ### Trajectory {{trajectory}} [Output Format] Put your final output in a JSON object enclosed by a json code block like: “‘json {{ "name": <skill-name>, "description": <skill-description>, "body": <skill-body> }} “‘ 

E.2 Skill Merge Prompt 

The following prompt instructs the skill composer to merge two related skills into a more general one. Skill Merge Prompt 

You are a professional Skill Merger. Askill is an executable procedure that provides specialized instructions for specific tasks. [Task Description] 1. Given two seperate skills, your task is to merge them into a more generalizable and reusable skill. 2. The merged skill should cover both the common and diverse instructions within the given two skills. The reusability/generalizability should be both model-level and task-level: - The skill should help current language model not only solve current question but this type of questions - The skill should help other language models not only solve current question but this type of questions [Skill Formulation] The generated skill should consist of three components including name, description and body: 1. **name**: The identifier of the skill. Once the name is generated, it will never be further modified. Therefore, make its name as general and abstract as possible, rather than limiting it to the current input problem and trajectory. 2. **description**: When to trigger, what it does. This is the primary triggering mechanism - include both what the skill does AND specific contexts for when to use it. All "when to use" info goes here, not in the body. 3. **body**: A document in markdown-style. It should contain the name of the skill and several sections with detailed instructions (i.e., what you want the skill to do and roughly how it should do it) to solve this type of tasks. [Skill Merge Instructions] 1. When merging two skills, try to explain 

to the model why things are important in lieu of heavy-handed musty MUSTs. 2. Use theory of mind and try to make the skill general and not super-narrow to specific examples. 3. The merged skill should cover both the common and diverse instructions within the given two skills. 4. Merge the two skills into one standalone, executable skill, not a simple combination. 5. Remove overlap and unify similar instructions into the clearest version. 6. Make the final result clearer, cleaner, more reusable and more generalizable than either original skill alone. [Input Details] ### Skill A —name: {name_a} description: {description_a} —{body_a} ### Skill B —name: {name_b} description: {description_b} —{body_b} [Output Format] Put your final output in a JSON object enclosed by a json code block like: “‘json {{ "name": <skill-name>, "description": <skill-description>, "body": <skill-body> }} “‘ 

E.3 Skill Improve Prompt 

The following prompt instructs the skill composer to refine an existing skill based on a new task tra-jectory. Skill Improve Prompt 

You are a professional Skill Improver. A skill is an executable procedure that provides specialized instructions for specific tasks. [Task Description] 1. Given a question, an original skill, and a corresponding trajectory generated by a language model when injecting this original skill into its context, your task is to generate an improved version of the skill. 2. The improved skill should maintain its reusability/generalizability at both model-level and task-level: - The skill should help current language model not only solve current question but this type of questions 

13 - The skill should help other language models not only solve current question but this type of questions [Expert Analysis Before Improvement] Before designing the improved skill, you must act as an expert evaluator: - Carefully examine the trajectory and judge whether it fully completed the user’s request. - Decompose the trajectory’s completion degree: identify specific parts that were executed well and parts that were insufficient or incorrect. - If the request was completed successfully, extract reliable, generalizable experiences (best practices) that likely contributed to the success, and check whether the current skill already captures them. - If certain aspects were not handled well, summarize the lessons learned and points of attention that the improved skill should explicitly warn about or guide the model to avoid. - Use this analysis to inform the improved skill, ensuring that the final skill encodes both proven strategies and preventive guidance. [Skill Formulation] The generated skill should consist of three components including name, description and body: 1. **name**: The identifier of the skill. Once the name is generated, it will never be further modified. Therefore, make its name as general and abstract as possible, rather than limiting it to the current input problem and trajectory. 2. **description**: When to trigger, what it does. This is the primary triggering mechanism - include both what the skill does AND specific contexts for when to use it. All "when to use" info goes here, not in the body. 3. **body**: A document in markdown-style. It should contain the name of the skill and several sections with detailed instructions (i.e., what you want the skill to do and roughly how it should do it) to solve this type of tasks. [Skill Improve Instructions] 1. Use the original skill as a foundation and produce an improved version that is clearer, more complete, and more reusable. 2. Preserve what already works, strengthen weak or missing guidance, and reflect the lessons from the trajectory. 3. Keep the skill generalizable across similar tasks rather than overfitting to the current example. 4. If the trajectory does not provide useful improvement signal, return the original skill unchanged. [Input Details] Here are the question, the trajectory, and the original skill: ### Question 

{{question}} ### Trajectory {{trajectory}} ### Skill —name: {name} description: {description} —{body} [Output Format] Put your final output in a JSON object enclosed by a json code block like: “‘json {{ "name": <skill-name>, "description": <skill-description>, "body": <skill-body> }} “‘ 

E.4 Skill Use System Prompt 

The following system prompt equips the skill ex-ecutor with a skill to guide task solving. Skill Use Prompt 

You are a professional skill user. The following is a skill that provides specialized instructions for specific tasks. name: {name} —{body} Read the skill above carefully, and use its instructions to solve problems. If you think the skill is unuseful or even wrong, ignore it and use your own knowledge instead. 

F Case Studies 

Figure 7 gives a case study on how skill evolves before and after Skill Improve. With the improved skill, the skill executor get higher pass@ k on the same task. 14 # solve_quadratic_budget_allocation 

# Problem Type: Optimization / Greedy Selection with Convex Costs 

#

## Overview 

When faced with a constraint like "maximize total units" subject to "total cost <= M", 

and where the cost to acquire k items from a source i is k^2 * P_i (or generally 

convex), the most ef fi cient units to buy are those with the lowest marginal cost. 

For product i, the marginal cost of the 1st unit is P_i, 2nd is 3*P_i, 3rd is 5*P_i, etc. In 

general, the j-th unit costs (2*j - 1)*P_i. 

Instead of enumerating all units (which could reach billions), we determine a global 

**cost threshold** (the price of the last unit we afford) and calculate the quantities 

derived from it. 

## Steps [Over-long Steps Illustration] 

[Omit ~1300 tokens] 

**Summary Algorithm:** [Redundant Skill Summary] 

1. Sort `P` (optional optimization). 

2. Binary search for `V` in `[0, M]`. Maintain `min_cost` function that returns 

`sum(P[i]*x_i^2)` and `sum(x_i)` for given `x_i` derived from `V`. 

3. Find max `V` s.t. `min_cost(V).cost <= M`. 

4. Take base answer `base_ans = count_from_V(V)`. 

5. Compute `rem_budget = M - cost_from_V(V)`. 

6. Try incrementing `V` slightly or re-evaluating `count` for `V+1`. If `cost_for(V+1) 

> M`, we can potentially add fractional part of `(V+1)` group. Add ` fl oor(rem_budget / 

(V+1))` (bounded by actual availability of `(V+1)` items). 

7. Output total. 

# Quadratic Budget Allocation Optimization 

## Overview 

This skill addresses resource allocation problems where multiple categories of items e

xist, each with a quadratic cost function. Unlike linear costs (where you simply pick ch 

eapest per-

unit), here the marginal cost of subsequent units increases linearly (e.g., fi rst unit costs 

P, second costs 3P, third 5P...). The objective is to maximize total items purchased give 

n a fi xed budget M. 

## Prerequisites & Constraints [Detailed Requirements] 

-

**Time Complexity Requirement:** Solution must scale to N <= 2*10^5 items and 

M <= 10^18 budget within typical CP limits (~2 seconds). 

- **Integer Precision:** All calculations must support 64-

bit integers; Python handles arbitrarily large integers automatically, ensuring safety ag 

ainst over fl ow during intermediate summations. 

## Implementation Steps [Wrought Step Illustration] 

[Omit ~600 tokens…] 

## Edge Cases & Best Practices [Valuable Experience] 

- **Empty List:** If `N=0` or `M=0`, answer is 0. 

- **Large Values:** Ensure no division by zero (check `P[i] !

= 0`) and handle big int operations if using languages without native BigInt support. 

-

**Performance:** Avoid recomputing `P[i]` divisions unnecessarily. Sorting `P` initia 

lly can optimize fi nding terms quickly but linear scan is generally suf fi cient for `O(N l

og M)` given constraints. 

## Example Walkthrough [Detailed Example] 

**Input:** N=3, M=9, P=[4, 1, 9] 

1. Filter: All valid. 

2. BS Range [0, 9]. Try mid=4. 

- For P=4: k= fl oor((4//4+1)/2)=1. Cost=4.*1=4. 

- For P=1: k= fl oor((4//1+1)/2)=2. Cost=1.*4=4. 

- Total Cost=8 <= 9. Feasible. `best_V`=4. Low=5. 

3. Continue BS until converging around 4. 

4. Base Units = 3 (1 from P=4, 2 from P=1). Cost=8. 

5. Remainder = 1. 

6. Next Cost candidate (V+1=5). Available P=1 units at cost 5? Yes. Diff Count > 0. 

7. Can afford 5? Rem(1)//5 = 0. 

8. Final Answer = 3. 

Before Skill Improve: Pass@k=0.0  After Skill Improve: Pass@k=1.0 Figure 7: A case study on under-improved skill (left) and improved skill (right). 

15