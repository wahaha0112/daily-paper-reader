---
title: "Skill is Not One-Size-Fits-All: Model-Aware Skill Alignment for LLM Agents"
title_zh: 技能并非千篇一律：面向LLM智能体的模型感知技能对齐
authors: "Jianxiang Yu, Jiapeng Zhu, Bochen Lin, Qier Cui, Zichen Ding, Xiang Li"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.30723v1"
tags: ["query:skill"]
score: 9.0
evidence: LLM智能体的模型感知技能对齐
tldr: 大型语言模型智能体在执行长序列任务时经常检索外部技能库。但现有技能库忽视不同模型背板的能力差异，同一技能对某些模型有益却可能损害其他模型。本文提出MASA框架，通过分层技能进化流水线和轻量级模型条件重写器，自动将技能适配到目标模型而不修改其权重。实验表明MASA在多个环境和模型上一致取得最优性能，最高比最强基线提升25.8分，且训练的重写器能零搜索泛化到新任务，推理成本远低于大型教师模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30723v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 1254, \"height\": 1254}]"
motivation: 现有技能库被视为模型无关，但实验表明技能有效性高度依赖模型，同一技能对不同模型效果迥异。
method: 提出MASA框架，两阶段：第一阶段用爬山和UCB树搜索迭代重写技能；第二阶段训练轻量级模型条件重写器单步生成适配技能。
result: 在三个交互环境和四个模型上，MASA平均性能最佳，最高提升25.8分，重写器泛化到未见任务且优于大型教师LLM。
conclusion: 模型感知的技能对齐无需修改权重即可提升智能体性能，且高效可泛化，为技能库适配提供新范式。
---

## 摘要
LLM智能体越来越多地检索外部策展的技能——在决策时检索的程序性指令——以提升在长周期交互任务中的表现。现有技能库通常被视为模型无关的，在不同能力和行为的骨干模型间复用相同的技能表述。然而，我们在多个模型规模上的控制实验表明，技能有效性强烈依赖模型：对一个骨干模型有益的技能可能损害另一个。受此观察启发，我们提出MASA（模型感知技能对齐）框架，该框架在不修改智能体权重的情况下，使技能适应每个目标骨干模型。MASA分为两个阶段：（1）分层技能演化流程，利用爬山算法和UCB驱动的树搜索，在环境反馈和模型能力概况的引导下，迭代重写通用和任务特定技能；（2）在演化轨迹上训练的轻量级模型条件技能重写器，通过单次前向传播再现适应过程。在三个交互环境和四个骨干模型上的实验表明，MASA一致实现了最佳整体性能，相比最强基线最高提升25.8个点。学习到的重写器进一步泛化到未见任务和环境，无需额外搜索，以极低的推理成本持续超越更大的教师LLM。

## Abstract
LLM agents increasingly retrieve externally curated skills-procedural instructions retrieved at decision time-to improve performance on long-horizon interactive tasks. Existing skill libraries are typically treated as model-agnostic, reusing the same skill formulations across backbones with substantially different capacities and behaviors. However, our controlled experiments across multiple model scales show that skill effectiveness is strongly model-dependent: a skill that benefits one backbone can harm another. Motivated by this observation, we propose MASA Model-Aware Skill Alignment, a framework that adapts skills to each target backbone without modifying agent weights. MASA operates in two stages: (1) a hierarchical skill evolution pipeline that iteratively rewrites general and task-specific skills using hill climbing and UCB-driven tree search, guided by environment feedback and model capability profiles; and (2) a lightweight model-conditioned skill rewriter trained on evolution trajectories to reproduce the adaptation in a single forward pass. Experiments across three interactive environments and four backbones show that MASA consistently achieves the best overall performance, with gains of up to 25.8 points over the strongest baseline. The learned rewriter further generalizes to unseen tasks and environments without additional search, consistently outperforming a much larger teacher LLM at a fraction of the inference cost.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：现有LLM智能体使用的技能库（skill library）通常被视为“模型无关”（model-agnostic），即同一套技能在不同能力、不同规模的骨干模型（backbone）上被直接复用。然而，实际部署中模型规模差异巨大（如从4B到32B），一个技能对某一模型有益，却可能主动损害另一模型。作者通过受控实验验证了这一“一刀切”假设的失败。
- **研究动机**：技能的有效性高度依赖于目标模型的能力、默认推理策略和行为模式。因此，需要一种**模型感知（model-aware）** 的技能对齐方法，在不修改模型权重的条件下，将技能的表达方式适配到每个特定骨干模型，从而释放其潜力。
- **整体含义**：技能库的设计不应仅关注内容，更应关注表达形式是否与目标模型兼容。将技能视为“模型感知的工件”，通过自动搜索和轻量级重写实现按需适配，可以显著提升智能体性能，并使小模型也能展现大模型级别的行为能力。

#### 2. 方法论：核心思想、关键技术细节

- **核心思想**：将技能对齐（skill alignment）视为一个分层搜索问题，由环境反馈和模型能力概况（model card）共同驱动。提出**MASA**框架，包含两个互补组件：
  - **① 分层模型条件技能演化（Hierarchical Model-Conditioned Skill Evolution）**：使用更强的教师LLM（DeepSeek-V4-Pro）迭代重写技能，重写过程以目标骨干的**模型卡**（记录架构、训练来源、能力强弱项）为条件信号。
  - **② 轻量级模型条件技能重写器（Model-Conditioned Skill Rewriter）**：从演化轨迹中学习重写策略，部署时只需单次前向传播即可完成适配，无需环境交互。
- **关键技术细节**：
  - **技能库结构**：分为通用技能（跨任务策略原则）和任务特定技能（按任务类型的行动指南）。推理时分别检索。
  - **调整奖励函数**：`R = SR - λ·NHR`，其中SR任务成功，NHR为“无进展步率”，惩罚技能导致的无效徘徊。
  - **阶段1：通用技能演化（爬山法）**：在全部任务类型上滚动评估，收集失败轨迹，由教师分析并重写通用技能集，若新集奖励更高则接受。最大迭代10次，容忍连续3次无改进则停止。
  - **阶段2：任务特定技能演化（每类型UCB树搜索）**：对每个任务类型独立构建树，节点为候选技能集，边为教师重写。使用UCB1选择节点扩展、评估并反向传播奖励。每类型迭代10次，每次评估N=100个回合。
  - **技能重写器训练**：以Qwen3-4B为骨干，通过监督微调（SFT）训练，输入（模型卡、输入技能集、任务描述），输出演化得到的最优技能。训练数据包含多种来源的输入（搜索中间状态、跨模型迁移、单次教师重写、噪声扰动等），覆盖广泛质量范围。
  - **推理**：单次前向传播即生成适配技能，无需环境交互。

#### 3. 实验设计

- **使用场景/数据集**：
  - **ALFWorld**：文本家庭任务（6种任务类型），成功率和步数。
  - **WebShop**：在线购物仿真，成功率和分数。
  - **Search-augmented QA**：包含单跳（NQ, TriviaQA, PopQA）和多跳（HotpotQA, 2Wiki, MuSiQue, Bamboogle）共7个问答基准。
- **基准方法**：
  - **No Skill**：无任何技能。
  - **Base Skill**：来自SkillRL的初始技能库，对所有骨干相同。
  - **DS-Adapter**：直接用教师LLM（DeepSeek-V4）基于模型卡进行单次重写，无迭代搜索。
  - **MASA**（本文方法）：包含完整演化流水线及重写器。
- **目标骨干**：Qwen3家族（4B/8B/14B/32B，非思维模式），教师LLM为DeepSeek-V4-Pro。额外在Gemma3家族（4B/12B/27B）上做验证实验。
- **重写器变体**：训练时分为**Cross-env**（仅在Search+WebShop数据上训练）和**Cross-task**（额外加上ALFWorld中非保留任务类型的数据）。

#### 4. 资源与算力

- **文中未明确说明**：没有给出GPU型号、数量、训练时长等具体算力信息。
- **间接线索**：
  - 教师模型为DeepSeek-V4-Pro，属大型闭源模型，通过API调用（成本较高）。
  - 技能演化流水线需要数百至上千次全环境滚动（环境交互），但未估算具体算力。
  - 技能重写器基于Qwen3-4B（轻量级），采用full-parameter SFT，AdamW优化器，学习率1e-5，5个epoch，batch size 4，梯度累积4，序列长度4096，BF16精度。训练数据规模为数百到近千条（如Combined Rewriter 769条，Cross-env 499条），整体计算量相对较低。

#### 5. 实验数量与充分性

- **实验数量**：丰富且系统。
  - 主要结果（表1、表2）：涵盖3个环境×4个骨干×4个方法，每个骨干有平均成功率和组件指标。
  - 消融实验（表3a,b）：分析两阶段搜索各自贡献、重写器模型卡的必要性。
  - OOD泛化实验（图3、图5）：跨环境、跨任务迁移，保留3个ALFWorld任务类型，对每个骨干对比多个变体。
  - 初步研究（图1、表4、表5、表6）：在Qwen3和Gemma3上系统改变技能粒度，验证模型依赖性。
  - 附加分析（附录C、G）：失败案例分析、WebShop轨迹统计、定性示例（表9）。
- **充分性与公正性**：
  - 实验条件控制较好：同一技能库变体仅改变粒度（保持内容一致），对比公平。
  - 基线设置合理：覆盖无技能、通用技能、单次教师重写。
  - 环境多样（文本交互、网页、知识检索），任务类型丰富（单跳/多跳QA、家用任务）。
  - 每个方法均报告每个骨干下的平均成功率和/或组件分数，并标注最优。
  - 重写器评估时采用留出任务类型，避免数据泄漏。
  - 不足：主要限于Qwen3家族，Gemma3仅用于初步验证，未在其他开源或闭源模型上全面评估演化流水线。

#### 6. 论文的主要结论与发现

- 同一技能库对不同规模模型效果非单调且模型依赖，简单启发式（如为小模型提供更简洁的指导）不成立。
- MASA在所有环境和骨干上均取得最佳成功率，最高比最强基线提升**25.8个点**（Qwen3-8B在ALFWorld上）。
- MASA不仅提升成功率，还减少交互步骤（效率提升）。
- 对于习惯于生成冗长思维链的大模型（如WebShop中的Qwen3-14B/32B），MASA能通过适配技能纠正无效行为模式。
- MASA-Rewriter（仅4B参数）在未见任务和环境中超越教师LLM（DeepSeek-V4）的one-shot重写，且推理成本极低。
- 消融实验表明通用技能和任务特定技能搜索各有其重要性，且模型卡对重写器提供关键条件信号。

#### 7. 优点

- **创新性**：首次系统揭示技能-模型“对齐”问题，并提出完整的搜索+蒸馏解决方案。
- **实用性**：轻量级重写器可在单次前向传播中完成适配，无需环境交互，便于部署。
- **鲁棒性**：跨多个环境、多个骨干、以及跨任务/跨环境的泛化性均得到验证。
- **效率**：MASA同时提升成功率并减少交互步数，降低推理成本。
- **可解释性**：通过模型卡提供结构化的条件信号，演化过程可通过失败分析和树搜索追溯。
- **完整性**：包含丰富的消融、泛化实验及定性分析，支持了核心主张。

#### 8. 不足与局限

- **模型覆盖面有限**：主要实验局限于Qwen3家族（4B–32B），Gemma3仅用于初步验证。未在更广泛的开源模型（如Llama、Mistral）或闭源模型（GPT-4o、Claude）上系统评估演化流水线（受限于API成本）。
- **依赖环境奖励信号**：演化流水线依赖环境提供的自动成功/失败信号（如任务完成标志），对于无此类信号的开放任务（如真实世界应用）需要额外设计外部评估器或人工标注。
- **可能引入偏差**：技能演化过程中可能继承教师模型或失败轨迹中的偏见或不安全启发，需要人工审查。
- **重写器训练数据规模较小**（数百条），尚需验证在更大规模和更多样化场景下的表现。
- **安全性考虑**：增强智能体能力的同时也可能带来风险，在安全敏感场景需谨慎部署。

（完）
