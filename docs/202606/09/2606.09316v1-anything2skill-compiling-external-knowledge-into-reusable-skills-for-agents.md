---
title: "Anything2Skill: Compiling External Knowledge into Reusable Skills for Agents"
title_zh: Anything2Skill：将外部知识编译为智能体可复用的技能
authors: "Qianjun Pan, Yutao Yang, Junsong Li, Jie Zhou, Kai Chen, Xin Li, Qin Chen, Liang He"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09316v1"
tags: ["query:skill"]
score: 9.0
evidence: 将外部知识编译为Agent可复用技能的框架
tldr: "检索增强生成（RAG）让智能体在推理时访问外部知识，但只能检索碎片化声明性证据，无法复用过程性知识。本文提出Anything2Skill框架，将异构外部知识（如手册、示例、日志）编译成可检索、可执行的技能。方法包括证据窗口分解、计划扩展技能提取、结构化技能契约转换和分类感知技能库管理。在qs和GitHub-CLI任务上结合RAG分别达到98.85%和94.10%成功率，显著优于纯RAG。该工作将隐性过程知识显式化为技能，推动智能体从知识访问向能力重用转变。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09316v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1587, \"height\": 893, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09316v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 778, \"height\": 264, \"label\": \"Table\"}]"
motivation: 现有RAG仅提供碎片化声明性知识，智能体需从外部知识反复推断过程性技能，限制了能力复用。
method: 提出Anything2Skill框架，先分解知识记录为证据窗口，在技能树先验下进行计划扩展提取，再转换为结构化技能契约，最后通过分类感知编译存入持久化技能库。
result: "在qs和GitHub-CLI上，Anything2Skill结合RAG分别实现98.85%和94.10%成功率，相比纯RAG提升显著。"
conclusion: 将异构外部知识中的隐性过程知识编译为显式可复用技能，是扩展检索增强智能体从知识访问到能力重用的有效途径。
---

## 摘要
检索增强生成（RAG）使智能体能够在推理时访问外部知识，但它主要检索零散的陈述性证据，导致智能体需要反复从文章、手册、示例、日志或轨迹中推断任务流程。这引出一个基本问题：能否将从外部知识库中提取的技能安装到智能体中，使其能够快速逼近领域专家能力？在本文中，我们提出Anything2Skill，一种基于分类学引导的框架，将异构外部知识编译为智能体可复用、可检索且可执行的技能。给定知识记录语料库，Anything2Skill首先将每条记录分解为证据窗口，并在技能树先验下执行规划与扩展的技能提取。提取的候选技能随后转换为结构化技能合约，明确调用条件、禁忌、动作序列、工作流步骤、约束、输出规范、支持证据和置信度分数。为构建可部署的程序性记忆，Anything2Skill通过分类学感知编译、注册表级协调、生命周期追踪、版本化更新和可视化技能树投影，将提取的技能持久化存储在SkillBank中。推理时，智能体同时从原始知识库检索任务相关段落，并从SkillBank检索相关程序性技能，使得RAG提供陈述性证据，而编译后的技能提供可复用的程序性指导。在qsv和GitHub-CLI上的实验表明，结合RAG的Anything2Skill分别达到98.85%和94.10%的成功率，显著优于纯RAG智能体。这些结果表明，将潜在程序性知识编译为显式技能是从知识访问扩展到能力复用的有效方式。

## Abstract
Retrieval-augmented generation (RAG) enables agents to access external knowledge at inference time, but it primarily retrieves fragmented declarative evidence, leaving agents to repeatedly infer task procedures from passages, manuals, examples, logs, or trajectories. This raises a fundamental question: can skills extracted from external knowledge bases be installed into an agent, enabling it to rapidly approximate domain expertise? In this paper, we propose Anything2Skill, a taxonomy-guided framework that compiles heterogeneous external knowledge into reusable, retrievable, and executable skills for agents. Given a corpus of knowledge records, \textsc{Anything2Skill} first decomposes each record into evidence windows and performs plan-and-expand skill extraction under a skill-tree prior. The extracted candidates are then converted into structured skill contracts that specify invocation conditions, contraindications, action moves, workflow steps, constraints, output specifications, supporting evidence, and confidence scores. To construct a deployable procedural memory, Anything2Skill manages the extracted skills in a persistent SkillBank through taxonomy-aware compilation, registry-level reconciliation, lifecycle tracking, versioned updates, and visible skill-tree projection. At inference time, agents retrieve both task-specific passages from the original knowledge base and relevant procedural skills from the SkillBank, allowing RAG to provide declarative evidence while compiled skills provide reusable procedural guidance. Experiments on qsv and GitHub-CLI show that Anything2Skill combined with RAG achieves 98.85\% and 94.10\% success rates, respectively, substantially outperforming RAG-only agents. These results suggest that compiling latent procedural knowledge into explicit skills is an effective way to extend retrieval-augmented agents from knowledge access toward capability reuse.

---

## 论文详细总结（自动生成）

## Anything2Skill：将外部知识编译为智能体可复用的技能 — 中文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：检索增强生成（RAG）虽然能让智能体在推理时访问外部知识，但仅提供零散的**陈述性证据**（如段落、手册片段），智能体仍需从这些碎片化信息中反复**推断任务流程**（程序性知识），导致**能力复用效率低**。现有技能系统多依赖手动设定、环境交互或任务时生成的代码，未直接挖掘外部知识库中已蕴含的**潜在程序性知识**。
- **整体意义**：该论文提出一种**从外部知识到可复用技能的编译范式**，将异构记录（文档、手册、日志、对话、轨迹）中的隐性程序性知识显式化为结构化、可检索、可执行的技能，构建持久化的**程序性记忆**（SkillBank）。这使得智能体不仅能“访问知识”，更能“复用能力”，从而推动检索增强智能体从知识访问向能力重用的转变。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：  
  基于**技能分类树先验**，从异构知识记录中提取并验证程序性技能；通过**证据窗口分解**、**计划-扩展提取**、**结构化技能合约**生成；再经**分类感知编译**、**注册表级协调**、**生命周期管理**、**版本化更新**和**可视化技能树投影**，将技能持久化存入 SkillBank；推理时与 RAG 结合，使陈述性证据与程序性指导互补。

- **关键技术细节**：
  - **证据窗口**：将每个记录拆分为严格边界窗口 `wij = ⟨文本, 标题上下文, 原跨度, 边界锚点, 元数据⟩`，确保技能全程可溯源。
  - **计划-扩展提取**：  
    - **计划阶段**：LLM 规划器从证据窗口识别候选技能（含目标、类型、调用提示等）。  
    - **扩展阶段**：独立将每个候选扩展为结构化技能草稿；若窗口不支持则返回空。  
    - **程序性有效性过滤**：仅保留包含工作流/动作/约束等程序性组件且能映射到分类树的候选。
  - **技能合约结构**：  
    `s = ⟨名称, 描述, 资产类型, 分类节点, 粒度, 调用条件, 禁忌, 动作序列, 工作流步骤, 约束, 输出规范, 证据集, 置信度⟩`
  - **文档级编译**：基于分类感知标识键对草稿分组，编译器优化覆盖度且避免冗余与无依据声明。
  - **注册表级协调**：对进入 SkillBank 的技能执行七大生命周期动作（CREATE / STRENGTHEN / REVISE / MERGE / SPLIT / UNCHANGED / DISCARD），通过混合检索（稠密+稀疏+分类兼容性）寻找相关技能并预测动作。
  - **可视化技能树投影**：按域/族/层级组织活跃技能，自动选择父节点，同级去重合并。
  - **推理增强**：同时检索原始知识库段落（RAG）和 SkillBank 技能，引导智能体执行。

### 3. 实验设计：数据集/场景、Benchmark、对比方法

- **Benchmarks**：
  - **qsv**：基于官方 qsv 文档的 CSV 命令行操作任务（命令选择、选项组合、表格转换）。  
  - **GitHub-CLI**：基于 GitHub CLI 手册的仓库管理操作（问题处理、PR 工作流、发布管理、仓库检查等），共 110 个源文档，编译出 179 个技能（1 全局、6 顶级、62 二级、110 微技能）。
- **对比方法**（四种上下文配置，均使用 `gpt5.4` 作为基础模型）：
  - **Base Agent**：无额外外部上下文。
  - **RAG**：检索原始知识库中 top-3 段落。
  - **Anything2Skill**：仅从 SkillBank 检索技能，不检索原始段落。
  - **Anything2Skill + RAG**：同时检索技能与原始段落。
- **评价指标**：任务成功率（自然语言指令翻译为正确可执行命令行操作的比例）。

### 4. 资源与算力

论文中**未明确说明**使用的 GPU 型号、数量、训练时长等具体算力信息。仅提及基于 `gpt5.4` 模型（LLM 作为提取、扩展、管理和推理组件），但所有实验均在该模型上完成，推断为 API 调用形式，未涉及大规模自训练。因此**算力成本未被量化详述**。

### 5. 实验数量与充分性

- **实验数量**：在两个基准上各对比了四种配置，共 8 组主实验。此外，论文通过分析详细讨论了**技能与 RAG 的互补性**、**技能单独提升**以及**复杂场景下的增益**，但**未单独列出消融实验**（如去除某一管理组件的影响）。实验规模相对充分，覆盖了技能单独作用、RAG 单独作用、两者结合三种关键情形。
- **充分性与公平性**：
  - 所有配置共用同一基础模型和任务协议，对比设置合理。
  - 实验验证了中心假设：技能编译能显著提升性能，且与 RAG 互补。
  - 不足在于：未在更多领域（如机器人、多模态、开放域问答）验证；未对技能提取质量进行人工评估；未考察不同技能管理策略（如不进行生命周期管理）的影响。总体而言实验设计**客观**，但**广度可进一步提升**。

### 6. 论文的主要结论与发现

1. **编译技能显著提升智能体性能**：在无检索段落时，使用 SkillBank 的 Anything2Skill 在 qsv 上提升 10.35 个百分点，在 GitHub-CLI 上提升 17.60 个百分点，证明 SkillBank 提供有意义的程序性指导。
2. **技能与 RAG 互补**：两者结合时性能最佳（qsv 98.85%，GitHub-CLI 94.10%），显著优于纯 RAG（95.41% / 76.50%），说明陈述证据与程序性指导解决不同执行需求。
3. **复杂工作流中技能优势更明显**：GitHub-CLI 增益大于 qsv，因为前者涉及更复杂的仓库管理流程，纯 RAG 不足以让智能体正确推断程序，技能合约直接提供结构化流程。
4. **潜在程序性知识可被有效编译**：许多有用的领域知识隐含在文档、手册等中，通过适当提取与组织可转化为可复用的智能体能力。

### 7. 优点

- **新颖的编译范式**：将外部知识库中的隐性程序性知识显式化为结构化技能合约，填补了 RAG 与技能学习之间的空白。
- **系统化的管理机制**：证据窗口分解、分类树约束、生命周期追踪、版本化更新、同级合并等，保证技能库的一致性和可维护性。
- **可解释性与可追溯性**：每个技能都附带明确的证据集、引用源、置信度，支持审计与调试。
- **兼容性强**：可直接叠加在现有 RAG 系统上，无需改变基础模型，迁移成本低。
- **实验验证充分**：两个实际命令行基准上的结果强力支持方法有效性，且分析清晰指出各成分贡献。

### 8. 不足与局限

- **提取质量依赖 LLM**：计划-扩展阶段和协调阶段都依赖 LLM（gpt5.4）的能力，若 LLM 本身对领域不熟悉或产生幻觉，可能导致技能错误。论文承认扩展阶段“若窗口不支持则返回空”，但未系统性分析幻觉风险。
- **实验领域有限**：仅测试了命令行工具操作（qsv 和 GitHub-CLI），未涵盖更广泛的智能体场景（如网页导航、机器人控制、医疗决策等）。泛化性未知。
- **缺乏定量消融实验**：未分别评估证据窗口大小、分类树结构、生命周期动作策略等组件的影响，也未比较不同管理策略的性价比。
- **计算成本未报告**：虽为 API 调用，但大量提取和管理步骤涉及多次 LLM 推理，具体推理次数、token 消耗等未被量化，难以评估实际部署成本。
- **技能检索的效率和可靠性**：文中使用混合检索公式（稠密+稀疏+分类兼容性），但未对检索成功率、延迟等做详细分析。技能库规模大时可能需优化。
- **长期维护风险**：虽设计了版本化与生命周期，但未讨论知识源更新后 SkillBank 如何自动或半自动同步，以及可能引入的冲突。

（完）
