---
title: "AIP: A Graph Representation for Learning and Governing Agent Skills"
title_zh: AIP：一种用于学习和管理智能体技能的图表示
authors: "Zachary Blumenfeld, Jim Webber"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04781v1"
tags: ["query:skill"]
score: 9.0
evidence: 将智能体技能表示为有向执行图
tldr: "现有Agent技能以自由形式文本描述，要求Agent在每次会话中重新推导行为，导致实现可靠性低且技能创建改进困难。AIP将技能建模为有向执行图，节点为确定性脚本或自然语言描述，节点间有显式类型输入输出边，并由YAML规范验证。编译器将现有技能转换为此形式，在SkillsBench的27个真实任务上，Claude Sonnet的平均任务奖励从0.60提升至0.71，通过率从53%提升至67%，统计显著。图结构支持逐节点诊断修复，使技能改进成为可测量的调优循环，并支持语料库级治理和强化学习动作空间。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04781v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 496}, {\"url\": \"assets/figures/arxiv/2606.04781v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1679, \"height\": 1173}, {\"url\": \"assets/figures/arxiv/2606.04781v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2606.04781v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1819, \"height\": 1665}, {\"url\": \"assets/figures/arxiv/2606.04781v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 867, \"height\": 1608}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04781v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 361}, {\"url\": \"assets/tables/arxiv/2606.04781v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1819, \"height\": 1665}, {\"url\": \"assets/tables/arxiv/2606.04781v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1687, \"height\": 1039}, {\"url\": \"assets/tables/arxiv/2606.04781v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 670, \"height\": 546}]"
motivation: 自由文本技能要求Agent重新推导代码和工具调用，导致可靠性低且编辑困难，尤其对模型训练不足的领域知识。
method: AIP将技能表示为有向执行图，节点为脚本或自然语言，边带输入输出类型，由YAML模式验证，并设计编译器转换现有技能。
result: "编译后Claude Sonnet任务平均奖励从0.60升至0.71，通过率53%到67%，统计显著；失败案例调整规范后恢复零回归。"
conclusion: AIP通过图结构提升技能可靠性与可改进性，支持语料库治理和强化学习动作空间。
---

## 摘要
当今的智能体技能主要由自由形式的散文构成，要求智能体在每个会话中阅读、解释并重新推导如何行动。这带来了两个叠加成本：在实现密集型任务上降低可靠性，以及在技能创建和改进方面的困难，因为编辑散文是一个脆弱的过程，人类和智能体都难以应对，尤其是对于模型训练中代表性不足的特定领域程序性知识。智能体指令协议（AIP）通过将技能建模为有向执行图来解决这两个问题：离散步骤作为节点，由确定性脚本或自然语言描述支持，通过显式类型的输入/输出边连接，并由模式验证的YAML规范管理。一个编译器元技能将现有的人类编写的技能转换为这种形式。其好处是双重的。首先，将人类编写的技能编译为AIP，使得Claude Sonnet在SkillsBench的27个真实智能体任务中的平均任务奖励从0.60提高到0.71，通过率从53%提高到67%——这是一个统计上显著的提升（Wilcoxon符号秩检验p=0.011），在13个平局的情况下赢得了12个任务，输了2个，且通常耗时更少。该图向智能体提供经过验证的、可运行的单元，而不是要求它从自然语言中重新推导代码、命令和工具调用。其次，在创建和改进方面，由于每个技能都经过模式验证、功能可测试且可按节点寻址，因此可以精确诊断和修复故障。两个已编写的技能故障被追溯到脚本层面。在调整AIP规范并重新编译后，两者均恢复且无回归（一个任务从0/5变为5/5），将技能改进转变为可测量的调优循环，而非散文重写。相同的图结构支持语料库级别的治理和技能内省，并为基于技能的强化学习提供了自然的动作空间。

## Abstract
Agent Skills today consist largely of free-form prose requiring the agent to read, interpret, and re-derive how to act in every session. This imposes two compounding costs: reduced reliability on implementation-heavy tasks, and difficulty in skill creation and improvement, since editing prose is a fragile process that both humans and agents struggle with, particularly for domain-specific procedural knowledge underrepresented in model training. The Agent Instruction Protocol (AIP) addresses both by modeling a skill as a directed execution graph: discrete steps as nodes backed by deterministic scripts or natural-language descriptions, connected by explicit typed input/output edges, and governed by a schema-validated YAML specification. A compiler meta-skill translates existing human-written skills into this form. The benefits are twofold. First, compiling human-written skills to AIP raised Claude Sonnet's mean task reward from 0.60 to 0.71 and pass rate from 53% to 67% across 27 real agent tasks from SkillsBench - a statistically significant gain (Wilcoxon signed-rank p = 0.011), winning 12 tasks to 2 with 13 ties - often in less wall-clock time. The graph delivers vetted, runnable units to the agent rather than asking it to re-derive code, commands, and tool calls from natural language. Second, on creation and improvement, because each skill is schema-validated, functionally testable, and addressable node-by-node, failures can be diagnosed and repaired precisely. Two authored-skill failures were traced to the script level. After adjusting the AIP spec and recompiling, both recovered with zero regressions (one task going from 0/5 to 5/5), turning skill improvement into a measurable tuning loop rather than a prose rewrite. That same graph structure supports corpus-level governance and skill introspection, and provides a natural action space for reinforcement learning over skills.

---

## 论文详细总结（自动生成）

# 论文《AIP: A Graph Representation for Learning and Governing Agent Skills》中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题背景**：当前主流 Agent 技能（如 Anthropic 的 Agent Skills）以自由形式散文（SKILL.md）为核心表示，Agent 需要在每次会话中阅读、解释并重新推导代码、命令和工具调用。这种方式带来两大成本：
  - **可靠性低**：在实现密集型任务（implementation-heavy）上，Agent 重复推导易出错，且运行间差异大。
  - **技能创建与改进困难**：编辑散文是脆弱过程，人类和 Agent 都难以精确修改，尤其对于模型训练中代表性不足的领域知识。
- **核心动机**：将技能中可结构化的部分（确定性步骤、流程）从散文剥离，以有向执行图的形式存储，从而提升执行可靠性和可改进性。

## 2. 论文提出的方法论

### 核心思想
- 将技能建模为**有向执行图**：节点是离散步骤，每条边携带显式类型的输入/输出数据。
- 步骤节点分为两类：
  - **确定性脚本节点**：由可运行代码（如Python脚本）支持，负责计算任务。
  - **自然语言判断节点**：需人类类似判断或交互，由自然语言描述支持。
- 整个结构由 **模式验证的 YAML 规范** 管理，确保语法和类型正确性。

### 关键技术细节
- **图结构要素**：
  - `Step` 节点：声明 name、description、脚本路径、输入/输出（name, type）、控制依赖（depends_on, parallel, one_of）。
  - 边：`inputs`/`outputs` 显式连接，形成数据流。
  - 顶层元数据（触发条件、禁止条件、反模式、场景、模式、集成）作为类型化卫星节点。
- **编译器元技能**：自动将现有人类编写的散文技能（或指令/文档）编译为 AIP 图表示。编译过程中执行模式验证，捕获类型错误、缺失字段等，同时将自然语言中可计算的部分自动生成脚本。
- **执行方式**：Agent 在上下文中加载 YAML 图结构，按图拓扑推理执行，确定性步骤由脚本完成，自然语言步骤由 LLM 推理。

### 公式或算法流程
- 无显式数学公式；核心流程为“编译 → 验证 → 图执行 → 逐节点诊断 → 修复 → 重编译”。

## 3. 实验设计

### 数据集 / 场景
- **Benchmark**: SkillsBench，包含 94 个任务，覆盖 8 个领域（工业/物理系统、自然科学、软件工程等）。
- **任务样本**：27 个任务（24 个分层抽样 + 3 个补充任务），每个任务有自然语言指令、沙盒环境和程序化验证器。

### 对比方法
- **Human-curated**: 使用 SkillsBench 附带的人类专家编写的原始技能（主要是散文形式）。
- **AIP-from-curated**: 通过 AIP 编译器将人类技能编译为 AIP 图格式。
- **AIP-from-instruction**（试验性）：从任务指令直接编译，因性能低被排除。
- **No skill** 和 **Self-generated** 作为副条件（不用于主要对比）。

### 求解器
- 固定使用 **Claude Sonnet (claude-sonnet-4-6)** 作为 Agent 模型。
- AIP 编译阶段使用 **Claude Opus 4.7**。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量或训练时长。仅在运行时提到使用 Claude 系列商业 API，无本地算力信息。
- 编译和评估均通过 API 调用完成，未披露具体 token 消耗或计算成本。

## 5. 实验数量与充分性

### 实验数量
- 共 **27 个任务 × 2 个条件（human-curated vs. aip-from-curated）× 5 次独立试验** = 270 次运行。
- 额外包含改进实验（2 个技能修复案例， each 5 trials）。

### 充分性评估
- **优点**：采用分层抽样覆盖困难度、实现类型、结构类型；使用统计显著性检验（Wilcoxon 符号秩检验）；报告多种指标（奖励、通过率、时间、工具调用）。
- **不足**：
  - 每 cell 仅 5 次试验，统计力有限。
  - 任务样本较小（27 个），且 3 个补充任务使用不同协议版本。
  - 存在**格式-作者混淆**：AIP 提升可能源于编译器改进的脚本质量，而非图结构本身。
  - 仅在单一模型（Claude Sonnet）上验证，未推广到其他系列或弱模型。
  - 验证器多为全有/全无，导致大量平局，区分度不足。

## 6. 论文的主要结论与发现

- **执行可靠性提升**：
  - 平均任务奖励：0.599 → 0.705（+0.106），Wilcoxon p = 0.011，统计显著。
  - 通过率：53.3% → 67.4%。
  - AIP 在 27 个任务中赢得 12 个、平局 13 个、输掉 2 个。
- **执行速度**：平均墙钟时间从 585s 降至 510s，但未达到统计显著（p≈0.28），提升主要集中在原先散文为主的任务。
- **技能可改进性**：
  - 两个失败案例（offer-letter-generator 从 0/5 → 5/5；bike-rebalance 从 0.40 → 0.60）均通过节点级诊断和修复解决，无回归。
  - 表明图结构支持局部精确修复，而非散文整体重写。
- **治理与学习潜力**：图结构可投影到图数据库进行查询审计，并为强化学习提供有界的动作空间。

## 7. 优点

- **结构化表示**：将确定性步骤与判断步骤分离，并显式化数据流，减少 Agent 推理负担。
- **模式验证**：编译时强制类型和结构正确性，避免运行时错误。
- **节点可寻址**：故障可准确定位到单个节点/脚本，修复可隔离验证。
- **编译器元技能**：复用现有散文技能，降低采用门槛。
- **实验设计**：扩展 SkillsBench 为多模式对比，控制求解器一致，统计显著性评估。
- **前瞻性**：为技能改进、强化学习、语料库治理提供形式化基础。

## 8. 不足与局限

- **格式-作者混淆**：未分离图结构与脚本质量提升的各自贡献，缺少控制实验（例如将相同脚本用散文提供）。
- **统计力有限**：每条件仅 5 次试验，任务数少，p 值虽显著但可能不稳定。
- **模型泛化性不足**：仅使用 Claude Sonnet，未测试其他模型（如 GPT、Gemini、开源模型）。
- **任务样本偏差**：3 个补充任务使用早期协议版本，可能引入系统性差异；部分预算限制任务（如 Civ6）无结果信息。
- **验证器特性**：全有/全无验证器导致高方差和平局，减弱区分能力。
- **应用限制**：AIP 当前仅为规范，缺乏运行时协议强制执行，依赖 Agent 自身遵循图拓扑。
- **计算成本未量化**：未报告编译和执行的 token 成本、API 调用费用，影响实际部署决策。

（完）
