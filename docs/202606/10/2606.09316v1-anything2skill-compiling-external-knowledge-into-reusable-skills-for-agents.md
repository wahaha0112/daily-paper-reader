---
title: "Anything2Skill: Compiling External Knowledge into Reusable Skills for Agents"
title_zh: Anything2Skill：将外部知识编译为智能体可复用技能
authors: "Qianjun Pan, Yutao Yang, Junsong Li, Jie Zhou, Kai Chen, Xin Li, Qin Chen, Liang He"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09316v1"
tags: ["query:skill"]
score: 9.0
evidence: 将外部知识编译为智能体可复用技能
tldr: "检索增强生成（RAG）代理只能检索碎片化事实，缺乏过程知识复用能力。本文提出Anything2Skill框架，从异构外部知识中编译可复用、可检索、可执行的技能，构建结构化技能契约并存入SkillBank。在qsv和GitHub-CLI任务中，结合RAG分别达到98.85%和94.10%成功率，大幅超越纯RAG代理。该方法将代理从知识访问扩展到能力复用，有效提升领域专长。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09316v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1587, \"height\": 893}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09316v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 778, \"height\": 264}]"
motivation: 现有RAG只能检索零散声明证据，无法提取过程性技能，导致代理反复从知识中推断任务步骤。
method: 提出Anything2Skill，分解知识记录为证据窗口，利用计划-扩展提取技能候选，转化为结构化契约，并通过SkillBank进行编译、注册、版本管理。
result: "在qsv和GitHub-CLI上，Anything2Skill+RAG的代理成功率达98.85%和94.10%，显著高于纯RAG基线。"
conclusion: 将外部知识中潜在的过程性知识编译为显式技能，是扩展RAG代理从知识访问到能力复用的有效途径。
---

## 摘要
检索增强生成（RAG）使智能体能够在推理时访问外部知识，但其主要检索的是碎片化的陈述性证据，导致智能体需要反复从段落、手册、示例、日志或轨迹中推断任务流程。这引出一个根本性问题：能否将从外部知识库中提取的技能安装到智能体中，使其能够快速近似领域专业知识？本文提出Anything2Skill，一种基于分类学指导的框架，将异构外部知识编译为可复用、可检索、可执行的智能体技能。给定知识记录语料库，	extsc{Anything2Skill}首先将每条记录分解为证据窗口，并在技能树先验下执行“规划-扩展”技能提取。提取的候选技能随后被转换为结构化的技能契约，明确指定调用条件、禁忌、动作序列、工作流步骤、约束、输出规范、支持证据和置信度分数。为构建可部署的程序性记忆，Anything2Skill通过分类学感知编译、注册表级协调、生命周期追踪、版本化更新和可视技能树投影，将提取的技能持久化存储在SkillBank中。推理时，智能体既从原始知识库检索任务相关段落，也从SkillBank检索相关程序性技能，使得RAG提供陈述性证据的同时，编译技能提供可复用的程序性指导。在qsv和GitHub-CLI上的实验表明，Anything2Skill结合RAG分别实现了98.85%和94.10%的成功率，显著优于仅使用RAG的智能体。这些结果提示，将潜在程序性知识编译为显式技能，是将检索增强智能体从知识访问扩展到能力复用的有效途径。

## Abstract
Retrieval-augmented generation (RAG) enables agents to access external knowledge at inference time, but it primarily retrieves fragmented declarative evidence, leaving agents to repeatedly infer task procedures from passages, manuals, examples, logs, or trajectories. This raises a fundamental question: can skills extracted from external knowledge bases be installed into an agent, enabling it to rapidly approximate domain expertise? In this paper, we propose Anything2Skill, a taxonomy-guided framework that compiles heterogeneous external knowledge into reusable, retrievable, and executable skills for agents. Given a corpus of knowledge records, \textsc{Anything2Skill} first decomposes each record into evidence windows and performs plan-and-expand skill extraction under a skill-tree prior. The extracted candidates are then converted into structured skill contracts that specify invocation conditions, contraindications, action moves, workflow steps, constraints, output specifications, supporting evidence, and confidence scores. To construct a deployable procedural memory, Anything2Skill manages the extracted skills in a persistent SkillBank through taxonomy-aware compilation, registry-level reconciliation, lifecycle tracking, versioned updates, and visible skill-tree projection. At inference time, agents retrieve both task-specific passages from the original knowledge base and relevant procedural skills from the SkillBank, allowing RAG to provide declarative evidence while compiled skills provide reusable procedural guidance. Experiments on qsv and GitHub-CLI show that Anything2Skill combined with RAG achieves 98.85\% and 94.10\% success rates, respectively, substantially outperforming RAG-only agents. These results suggest that compiling latent procedural knowledge into explicit skills is an effective way to extend retrieval-augmented agents from knowledge access toward capability reuse.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有检索增强生成（RAG）系统仅能检索零散的陈述性证据（如文档片段），无法提供“如何执行任务”的过程性知识。智能体需要反复从手册、日志、轨迹等异构来源中推断任务步骤，效率低下，且难以积累可复用的能力。
- **研究动机**：填补RAG在过程性知识方面的空白，将外部知识库中蕴含的潜在程序性知识（Procedural Knowledge）提取出来，编译成显式、可复用、可检索的技能，使智能体能够快速近似领域专长。
- **整体含义**：推动检索增强智能体从“知识访问”迈向“能力复用”，为构建持续进化、可积累技能的智能体系统提供新范式。

### 2. 论文提出的方法论

- **核心思想**：通过一个两阶段管道——**技能提取**与**技能管理**——将异构外部知识（文档、对话、日志、轨迹等）转化为结构化的技能契约，并组织成持久的、可检索的SkillBank，在推理时同时提供陈述性证据和过程性指导。
- **关键技术细节**：
  - **技能契约**：每个技能表示为包含名称、描述、归一化资产类型、分类节点、调用条件/禁忌、动作序列、工作流步骤、约束、输出规格、支持证据和置信度分数的结构化对象。
  - **技能提取**：分解输入记录为证据窗口；利用**技能树先验**（Skill-Tree Prior）约束资产类型空间（如macro_protocol, session_skill, micro_skill, safety_rule, knowledge_reference）；采用**计划-扩展（Plan-and-Expand）**两阶段策略：先由LLM规划轻量级候选，再独立扩展为完整技能草案；通过**程序有效性过滤器**（包含步骤、动作或约束）过滤非过程性候选。
  - **技能管理**：
    - **文档级编译**：对同一文档内共享相同分类键（包括类型、节点、领域等）的草案进行分组，并约束编译保持类型不变，且仅引用组内证据。
    - **注册表级协调**：通过**混合检索**（稠密+稀疏BM25+分类兼容性）查找相似技能，由管理模型预测生命周期动作（创建、强化、修订、合并、拆分、不变、丢弃），确保跨类型层操作（如将安全规则合并到可执行技能）被禁止。
    - **版本追踪**与**生命周期状态机**（候选→草案→评估→活跃→关注→废弃→退休）。
    - **可视技能树投影**：将活跃技能按层级组织，通过父节点选择和同层去重进行导航优化。
- **算法流程（文字说明）**：
  1. 输入语料库K和分类树T。
  2. 对每条记录分解为证据窗口W。
  3. 在每个窗口上，使用LLM规划候选技能列表，再扩展为草案，经程序性过滤保留。
  4. 按分类键分组，使用编译器生成规范技能（最大化覆盖、最小化不支持和冗余）。
  5. 对于每个新技能，从现有SkillBank中检索相似技能，预测并执行生命周期操作。
  6. 在推理时，对查询q同时进行RAG检索（从K获取段落）和SkillBank检索（获取技能），合并后输入下游智能体。

### 3. 实验设计

- **数据集/场景**：两个命令行智能体基准（CLI agent benchmarks）：
  - **qsv**：基于官方qsv文档的CSV命令行操作任务，要求选择命令、组合选项、进行表格数据转换。
  - **GitHub-CLI**：基于GitHub CLI仓库操作手册和gh命令文档（110篇源文档），涵盖issue处理、PR工作流、发布管理、仓库检查等操作。从该语料编译出179个技能（1个全局技能、6个顶级技能、62个二级技能、110个微技能）。
- **对比方法**：四种上下文配置，均使用同一基础模型`gpt5.4`：
  - Base Agent：无额外外部上下文。
  - RAG：检索top-3原始知识条目。
  - Anything2Skill：仅检索SkillBank中的技能（不检索源文档）。
  - Anything2Skill + RAG：同时检索两种信息。
- **评估指标**：任务成功率（完成正确命令行操作的百分比）。

### 4. 资源与算力

- 论文中**未明确说明**所使用的GPU型号、数量、训练时长等详细计算资源。
- 仅提及基础模型为`gpt5.4`，推测使用了API调用而非自有训练集群，技能提取和管理过程也基于LLM，因此整体算力需求主要依赖外部API。

### 5. 实验数量与充分性

- **实验数量**：在2个基准上各进行4组对比（共8组实验结果），附带一个主表格（Table 1）。未包含更细粒度的消融实验（例如移除技能树先验、不同检索权重、不同生命周期动作等）。
- **充分性判断**：实验设计简洁有力，直接对比了有/无技能、有/无RAG以及两者结合，清晰验证了核心假设。但实验覆盖领域较狭窄（仅限CLI任务），且缺乏对技能质量（如精确率、召回率）的评估。未进行跨域泛化测试或更多数据集的验证。整体来说，实验**基本充分但不够全面**，对比公平（同模型、同协议），结果客观。

### 6. 论文的主要结论与发现

- **编译的技能显著提升智能体性能**：即使不检索源文档，Anything2Skill在qsv上将成功率从81.60%提升至91.95%，在GitHub-CLI上从64.70%提升至82.30%。
- **技能与RAG互补**：两者结合达到最佳效果——qsv上98.85%、GitHub-CLI上94.10%，均大幅超过纯RAG（95.41%和76.50%）。
- **对复杂工作流提升更大**：GitHub-CLI任务涉及更多元的工作流、约束和场景组合，技能提供的结构化过程性指导比纯检索文档更有效，因此SkillBank单独性能已超过纯RAG。
- **核心结论**：将外部知识中潜在的程序性知识编译为显式技能，是将检索增强智能体从知识访问扩展到能力复用的有效途径。

### 7. 优点

- **问题新颖且实际**：直接针对RAG缺乏过程性知识的痛点，提出一个完整的解决方案。
- **方法链条完整**：从证据窗口构建、技能提取、结构化契约、到管理编译、协调、版本化、树投影，形成端到端系统。
- **设计细节严谨**：包含分类树先验防止类型混淆、混合检索协调、生命周期操作约束等精细设计。
- **实验验证高效**：在两个真实CLI任务中取得显著且一致的优势，充分展示技能+RAG的互补价值。
- **论文表述清晰**：方法论部分给出形式化定义和约束条件，易于复现。

### 8. 不足与局限

- **实验覆盖不足**：仅评估了CLI领域，未涉及机器人、对话、网页交互等其他常见智能体场景，泛化性待验证。
- **缺乏消融实验**：未量化每个组件（如计划-扩展策略、分类树先验、生命周期动作预测等）的独立贡献。
- **算力资源未报告**：缺失训练/推理的算力开销信息，不利于成本评估。
- **依赖LLM作为核心引擎**：提取和管理过程的可靠性与成本受限于LLM，未讨论低资源/离线场景。
- **评估指标单一**：仅用任务成功率，未测量技能本身的质量（如完整性、准确性、版本一致性）、检索效率、迁移能力等。
- **潜在偏差风险**：源知识库可能包含错误或有偏的程序性描述，编译后技能可能继承并放大这些问题。
- **未讨论跨任务泛化**：技能是否能在不同知识域间迁移或复用未测试。

（完）
