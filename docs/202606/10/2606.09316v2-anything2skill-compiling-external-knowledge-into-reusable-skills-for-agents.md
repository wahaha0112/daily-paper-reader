---
title: "Anything2Skill: Compiling External Knowledge into Reusable Skills for Agents"
title_zh: "Anything2Skill: 将外部知识编译成智能体可复用的技能"
authors: "Qianjun Pan, Yutao Yang, Junsong Li, Jie Zhou, Kai Chen, Xin Li, Qin Chen, Liang He"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09316v2"
tags: ["query:skill"]
score: 9.0
evidence: 将外部知识编译为智能体的可重用技能
tldr: "当前检索增强生成（RAG）虽能让代理访问外部知识，但仅提供碎片化陈述性证据，代理需重复推断任务流程。本文提出Anything2Skill框架，将异构外部知识（如文档、示例、轨迹）编译为可重用的结构化技能，并通过技能树先验提取、技能合同标准化和持久SkillBank管理，实现程序知识显式复用。在qsv和GitHub-CLI任务上，结合RAG的代理成功率分别达98.85%和94.10%，远超纯RAG。该工作将代理从知识访问扩展到能力复用，显著提升复杂任务执行效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09316v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1587, \"height\": 893, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09316v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 778, \"height\": 264, \"label\": \"Table\"}]"
motivation: 现有RAG仅提供碎片化陈述性知识，代理需反复推理任务流程，效率低且易出错。
method: 提出Anything2Skill框架，通过证据窗口分解、规划扩展技能提取、技能合同结构化，再经分类法编译存入SkillBank，实现知识到技能的转化与管理。
result: "在qsv和GitHub-CLI任务上，Anything2Skill结合RAG分别达到98.85%和94.10%成功率，显著优于纯RAG基线。"
conclusion: 将潜在程序知识编译为显式技能，可有效扩展检索增强代理能力，实现从知识访问到能力复用的跃升。
---

## 摘要
检索增强生成（RAG）使智能体在推理时能够访问外部知识，但它主要检索碎片化的陈述性证据，导致智能体需要反复从段落、手册、示例、日志或轨迹中推断任务流程。这引出了一个根本性问题：能否将从外部知识库中提取的技能安装到智能体中，使其快速逼近领域专业知识？在本文中，我们提出了Anything2Skill，一个分类学引导的框架，将异构外部知识编译成可复用、可检索、可执行的智能体技能。给定一个知识记录语料库，Anything2Skill首先将每条记录分解为证据窗口，并在技能树先验下执行规划-扩展式技能提取。然后，将提取的候选技能转换为结构化的技能契约，其中指定了调用条件、禁忌、动作步骤、工作流程、约束、输出规范、支持证据和置信度分数。为了构建可部署的程序性记忆，Anything2Skill通过分类学感知编译、注册表级协调、生命周期跟踪、版本化更新和可见的技能树投影，将提取的技能管理在持久化的SkillBank中。推理时，智能体从原始知识库中检索特定任务段落，并从SkillBank中检索相关程序性技能，使得RAG提供陈述性证据，而编译后的技能提供可复用的程序性指导。在qsv和GitHub-CLI上的实验表明，Anything2Skill结合RAG分别达到了98.85%和94.10%的成功率，显著优于仅使用RAG的智能体。这些结果表明，将潜在的程序性知识编译为显式技能，是将检索增强智能体从知识获取扩展到能力复用的有效途径。

## Abstract
Retrieval-augmented generation (RAG) enables agents to access external knowledge at inference time, but it primarily retrieves fragmented declarative evidence, leaving agents to repeatedly infer task procedures from passages, manuals, examples, logs, or trajectories. This raises a fundamental question: can skills extracted from external knowledge bases be installed into an agent, enabling it to rapidly approximate domain expertise? In this paper, we propose Anything2Skill, a taxonomy-guided framework that compiles heterogeneous external knowledge into reusable, retrievable, and executable skills for agents. Given a corpus of knowledge records, \textsc{Anything2Skill} first decomposes each record into evidence windows and performs plan-and-expand skill extraction under a skill-tree prior. The extracted candidates are then converted into structured skill contracts that specify invocation conditions, contraindications, action moves, workflow steps, constraints, output specifications, supporting evidence, and confidence scores. To construct a deployable procedural memory, Anything2Skill manages the extracted skills in a persistent SkillBank through taxonomy-aware compilation, registry-level reconciliation, lifecycle tracking, versioned updates, and visible skill-tree projection. At inference time, agents retrieve both task-specific passages from the original knowledge base and relevant procedural skills from the SkillBank, allowing RAG to provide declarative evidence while compiled skills provide reusable procedural guidance. Experiments on qsv and GitHub-CLI show that Anything2Skill combined with RAG achieves 98.85\% and 94.10\% success rates, respectively, substantially outperforming RAG-only agents. These results suggest that compiling latent procedural knowledge into explicit skills is an effective way to extend retrieval-augmented agents from knowledge access toward capability reuse.

---

## 论文详细总结（自动生成）

# Anything2Skill 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：检索增强生成（RAG）虽然让智能体在推理时能访问外部知识，但检索到的内容主要是碎片化的**陈述性证据**（如文档段落），智能体仍需从这些片段中反复推断任务流程（**程序性知识**）。这导致效率低下，且无法直接复用已学过的操作顺序、约束条件等程序化能力。
- **研究动机**：能否将隐藏在现有异构知识库（文档、手册、对话日志、执行轨迹等）中的潜在程序性知识提取出来，编译成**可直接检索、可复用、可执行的技能**，使智能体快速获得领域专长，而不仅仅依赖推理时临时构造过程。
- **整体含义**：本文提出将智能体从“知识访问”扩展到“能力复用”，通过显式的技能编译填补 RAG 仅提供陈述性证据的不足。

## 2. 方法论

### 核心思想
- **Anything2Skill**：一个**分类学引导的框架**，将异构知识记录编译成结构化技能契约，存入持久化的 **SkillBank**（程序性记忆）。推理时智能体同时检索原始知识（RAG 提供陈述性证据）和 SkillBank（提供程序性指导）。

### 关键技术细节
- **技能树先验 (Skill-Tree Prior)**：定义紧凑的内部资产类型空间 `A = {macro_protocol, session_skill, micro_skill, safety_rule, knowledge_reference}`，分类学树结构约束提取过程产生程序性技能而非自由摘要。
- **证据窗口分解**：将每条输入记录拆分为严格的证据窗口，每个窗口包含文本跨度、标题上下文、段落范围、边界信息、元数据，确保技能可溯源。
- **规划-扩展提取 (Plan-and-Expand)**：两步法。先由 LLM 规划器从每个证据窗口识别候选技能（含轻量目标、资产类型、调用线索等）；再对每个候选独立扩展为结构化草稿（仅当窗口直接支持时才生成，避免幻觉）。
- **结构化技能契约**：每个技能包含名称、描述、规范化资产类型、分类节点、粒度、调用条件、禁忌、动作步骤、工作流步骤、约束、输出契约、支持证据集合、置信度。
- **技能管理**：包括文档级编译（用分类学感知键分组并规范化）、注册表级协调（使用混合检索 BM25 + 稠密相似度 + 分类学兼容性评定，预测生命周期动作如 CREATE/REVISE/MERGE 等）、版本化更新、可见技能树投影（按层级链接父技能，并同层合并重复技能）。
- **推理阶段**：对查询 q，分别从原始知识库检索证据段落 `C_q`，从 SkillBank 检索相关程序性技能 `S_q`，然后智能体 `A_θ` 以两者为条件生成答案/操作。

### 算法流程（文字描述）
1. 输入：知识语料库 K，技能分类学 T。
2. 对每条记录 Di，分解为证据窗口 Wij。
3. 对每个窗口，执行规划步骤（LLM 生成候选列表 Pij），然后对每个候选执行扩展步骤（生成结构化草稿 ˜sijk）。
4. 应用程序有效性过滤器（需至少包含一个程序性组件且能映射到分类学）。
5. 文档级编译：按分组键 γ 聚合草稿，生成规范技能。
6. 注册表级协调：用混合检索找到已有相关技能，预测生命周期动作，更新 SkillBank。
7. 构建可见技能树：按层级链接，合并重复，形成最终 SkillBank B。
8. 推理时：RAG 检索 C_q，SkillBank 检索 S_q，智能体联合使用。

## 3. 实验设计

- **数据集/场景**：
  - **qsv 基准**：针对 CSV 命令行操作，基于官方 qsv 文档，任务需智能体选择命令、组合选项、进行表格数据转换。
  - **GitHub-CLI 基准**：基于 GitHub CLI 仓库操作指南和 gh 命令文档，涵盖问题处理、PR 工作流、发布管理、仓库检查等 110 篇源文档，最终编译出 179 个技能（1 全局、6 顶层、62 二级、110 微技能）。
- **对比方法**：
  - **Base Agent**：无额外外部上下文。
  - **RAG**：每任务检索 top-3 原始知识库文档。
  - **Anything2Skill**：仅提供从 SkillBank 检索的技能，不检索原始文档。
  - **Anything2Skill + RAG**：同时提供技能和原始文档。
- **基础模型**：统一使用 `gpt5.4`。
- **评价指标**：任务成功率（自然语言命令转化为有效命令行操作的比例）。

## 4. 资源与算力

- 论文中**未明确提及**所使用的 GPU 型号、数量、训练时长等算力信息。仅说明使用 GPT-5.4 模型进行提取和管理，但未披露推理或训练阶段的硬件配置。

## 5. 实验数量与充分性

- **实验数量**：两个基准（qsv、GitHub-CLI），每个基准上对比 4 种配置（共 8 组实验）。主要结果以一张表格呈现（表 1）。
- **消融分析**：通过对比 Anything2Skill 单独与 RAG 单独、以及两者结合，有效证明了技能与 RAG 的互补性。此外，对 GitHub-CLI 的技能树统计和专业性分析有所提及。
- **充分性评估**：
  - **优点**：覆盖了两个不同复杂度领域的 CLI 任务，结果清晰显示增益；消融实验设计合理，直接回答了两个研究问题。
  - **不足**：未与其他技能学习或程序性知识提取方法（如 Voyager、Reflexion 等）进行横向对比；仅测试了命令行场景，未涉及更复杂的多步骤/多工具交互或开放式环境；未讨论模型大小或检索窗口数的影响；只使用一种基础模型（gpt5.4），泛化性有限。
- **客观性**：实验设置公开，但未提供多次运行的标准差或统计显著性检验，可能存在偶然性。

## 6. 主要结论与发现

1. **编译技能可独立改善智能体**：仅使用 Anything2Skill（不检索原始文档）在 qsv 上成功率从 81.60% 提升到 91.95%，在 GitHub-CLI 上从 64.70% 提升到 82.30%，证明 SkillBank 能提供有用的程序性知识。
2. **技能与 RAG 互补**：RAG 单独在 qsv 上达 95.41%，GitHub-CLI 上仅 76.50%；而 Anything2Skill + RAG 在两者上分别达到 98.85% 和 94.10%，显著优于任何单独方法，证明陈述性证据和程序性技能解决不同方面。
3. **技能对复杂工作流尤其重要**：在 GitHub-CLI（更多变、多步骤流程）增益更大，说明编译的程序性知识降低了任务时重建程序的负担。
4. **总体效果**：结合 SkillBank 与 RAG 成功将智能体从知识访问推向能力复用，为检索增强系统提供了新方向。

## 7. 优点

- **创新性**：首次系统提出将异构外部知识编译为结构化、可复用的技能契约，桥接了 RAG 的陈述性知识与技能库的程序性知识。
- **方法完整性**：涵盖从证据窗口分解、分类学引导提取、技能契约定义、多层次管理（编译、协调、版本化、可见投影）的完整流水线。
- **实用性强**：直接利用现有文档/日志，无需额外环境交互，可快速部署。
- **实验证据扎实**：在两个真实命令行基准上验证，消融实验清晰表明各自贡献，且“技能+RAG”组合达到很高绝对值（接近 99% 和 94%）。
- **与现有系统兼容**：SkillBank 可以作为插件与任何 RAG 系统结合，不改变原有流程。

## 8. 不足与局限

- **方法局限性**：
  - 依赖 LLM 进行规划-扩展提取，提取质量受限于模型能力，高幻觉风险（尽管尽量保守）。
  - 分类学预定义可能不够灵活，适应新领域需要手动调整资产类型和层级。
  - 技能管理流程较复杂，协调和树投影可能引入错误或噪声。
- **实验局限**：
  - 仅测试命令行工具场景，未覆盖通用推理、多模态或具身任务，泛化性未知。
  - 缺乏与现有技能学习/程序提取方法（如 Voyager、ReAct with memory、生成式代理）的直接比较。
  - 只使用一种基础模型（gpt5.4），未分析不同规模 LLM 的敏感性。
  - 未报告多次运行的标准差，无法评估结果的稳定性。
  - 资源消耗（提取阶段的 LLM 调用次数、计算成本）未被量化。
- **应用限制**：
  - 需要高质量、结构化的知识语料库（文档、日志等），稀疏或噪声大的数据可能无效。
  - 技能更新依赖后续的“管理”步骤，实时性可能受限。
  - 安全与伦理：编入的技能可能包含偏见或错误操作，缺乏安全审计机制。

（完）
