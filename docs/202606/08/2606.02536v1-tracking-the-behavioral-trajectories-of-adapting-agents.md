---
title: Tracking the Behavioral Trajectories of Adapting Agents
title_zh: 追踪适应型智能体的行为轨迹
authors: "Jonah Leshin, Manish Shah, Ian Timmis"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02536v1"
tags: ["query:skill"]
score: 9.0
evidence: 使用技能文件差异在嵌入空间中测量智能体特质
tldr: "现代代理的行为由技能文件等文本文件的编辑所引导，文件演变直接影响代理未来行为。本文提出将代理特质定义为文本嵌入空间中的方向，利用线性模型从标记的文件差异中学习特质向量，并通过投影编辑的嵌入差异来评分。在68个标注的“寻求敏感数据”特质差异对上，该方法达到91.2%分类准确率和0.82斯皮尔曼秩相关。该特质评估被嵌入代理间协议，使代理可通过可信中介互评技能更新，为追踪自适应代理行为提供有效手段。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02536v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 820, \"height\": 498, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02536v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 738, \"height\": 118, \"label\": \"Figure\"}]"
motivation: 现代代理的行为由文本文件编辑直接引导，文件演变需要定量测量特质变化以理解行为轨迹。
method: 将特质定义为文本嵌入空间方向，训练线性模型学习特质向量，通过投影编辑的嵌入差异进行评分。
result: "在68个标记差异对上，寻求敏感数据特质分类准确率91.2%，斯皮尔曼秩相关0.82。"
conclusion: 提出特质测量方法并集成到代理间协议，支持通过可信中介评估技能更新。
---

## 摘要
文本文件（如技能文件、记忆文件和行为配置文件）在定义现代智能体的行为方式中起着核心作用。通过人类或智能体自身的编辑，这些文件可能会随时间演变，直接引导智能体在未来交互中的行为。我们提出了一种方法和框架，用于测量智能体的“特质”，即将特质定义为文本嵌入模型嵌入空间中的方向。我们在标记的“之前”与“之后”技能文件差异上训练一个线性模型以学习特质向量，然后通过将任意技能编辑的嵌入差异投影到该向量上对其进行评分。在68个标记的技能差异对（针对寻求敏感数据的性向特质）上评估，我们的方法在留一交叉验证下达到了91.2%的符号分类准确率和Spearman等级相关系数ρ=0.82。我们将这种特质评估构建到一个更广泛的智能体间协议中，使得一个智能体能够通过可信中介评估另一个智能体的技能文件更新。

## Abstract
Text files such as skill files, memory files, and behavioral configuration files play a central role in defining how modern agents act. Through edits by humans or the agents themselves, these files may evolve over time, directly steering the agent's behavior in future interactions. We present a methodology and framework for measuring agent $traits$ by defining traits as directions in the embedding space of a text embedding model. We train a linear model on labeled "before" versus "after" skill file diffs to learn a trait vector, then score arbitrary skill edits by projecting their embedding diffs onto this vector. Evaluated on 68 labeled skill diff pairs for the trait of propensity to seek sensitive data, our method achieves 91.2% sign classification accuracy and a Spearman rank correlation of $ρ= 0.82$ under leave-one-out cross-validation. We build this trait evaluation into a broader agent-to-agent protocol that enables one agent to evaluate another's skill file updates through a trusted intermediary.

---

## 论文详细总结（自动生成）

好的，这是对您提供的论文《追踪适应型智能体的行为轨迹》的详细中文总结。

## 论文总结：追踪适应型智能体的行为轨迹

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代AI智能体的行为由可编辑的文本文件（如技能文件、记忆文件）直接定义和引导。这些文件会随时间和交互不断演变（被人类或智能体自身编辑），但缺乏系统的、自动化的方法来追踪和量化这些演变如何改变智能体的关键特质（如安全性、数据寻求倾向）。
*   **研究动机**：
    *   **安全风险**：文本文件的编辑（尤其是恶意注入）可能潜移默化地改变智能体行为，带来严重安全风险。例如，Cisco研究员展示了对Claude Code记忆文件的攻击。
    *   **监控需求**：智能体在部署后会频繁适应（学习新技能、更新记忆），绝大多数编辑是良性的，但需要可靠的方法从海量更新中甄别出少数危险变化。
    *   **自治评估**：随着智能体自主性增强，一个智能体自动评估另一个智能体的行为特性（基于其源文件），而无需人类干预，成为多方协作信任的关键。
*   **整体含义**：本文旨在构建一种能够追踪智能体“行为轨迹”的方法和框架，以便在智能体系统持续演化的过程中，对其进行安全、可信的监控和评估。

### 2. 论文提出的方法论：核心思想、关键技术细节
*   **核心思想**：将智能体的“特质”（如寻求敏感数据的性向）定义为**文本嵌入空间中的一个方向**。通过分析技能文件编辑“前后”的嵌入向量差异，来量化特质的改变。
*   **关键技术细节**：
    1.  **特质向量学习**：
        *   **输入**：一组已标注的技能文件编辑对 `(B_i, A_i)` 和对应的连续标签 `y_i` (范围 [-1, 1]，表示特质变化的程度和方向）。
        *   **嵌入**：使用文本嵌入模型 `E`（如Qwen3-Embedding-8B）将 `B` 和 `A` 分别编码为单位长度的嵌入向量 `ê(B)` 和 `ê(A)`。
        *   **差异向量**：计算编辑后的差异向量 `d_i = ê(A_i) - ê(B_i)`，并同样归一化为单位向量 `d̂_i`。
        *   **回归训练**：使用Ridge回归，从归一化的差异向量 `d̂_i` 拟合到标签 `y_i`，学习得到的权重向量 `w` 即为所求的“特质向量”。
    2.  **特质评分（推断）**：
        *   对于一个新的编辑对，计算其归一化差异向量 `d̂` 与特质向量 `w` 的点积，再加上截距 `b`，即 `score = d̂·w + b`，得到一个标量分数。
    3.  **核心优势**：
        *   只关注文件*变化*的语义方向，而非文件的全部内容，有效减少了无关信息的干扰，提高了对特质变化的敏感性。
        *   使用向量投影进行评分，速度快、结果确定、可审计。

### 3. 实验设计：数据集、基准与对比方法
*   **数据集**：
    *   **来源**：从公开的 *Awesome Copilot* 仓库中收集了 63 个公共智能体技能文件作为“之前”版本。
    *   **构建**：为每个“之前”版本人工合成了一个“之后”版本，明确地增加了或减少了其“数据寻求”特质，生成了68个编辑对（其中5个项目生成了两个相反的编辑对）。
    *   **标注**：由作者和经过人工审查的LLM（Claude Opus 4.6）共同赋予连续的标签 `y_i` (-1 表示强烈减少数据寻求，+1 表示强烈增加)。
*   **要测量的特质**：Propensity to seek sensitive data（寻求敏感数据的性向）。
*   **评估基准（Baselines）**：
    1.  **YARA签名规则**：一个基于关键词匹配的传统规则方法。
    2.  **前沿LLM（GPT-5.4）**：直接将其作为分类器，判断编辑是增加还是减少了数据寻求特质。
*   **核心评估指标**：
    *   **符号分类准确率**：预测特质变化方向（+/−）的正确率。
    *   **Spearman等级相关系数**：衡量预测分数与真实标签之间排序一致性的能力。

### 4. 资源与算力
*   论文**没有明确说明**训练特质向量（即训练Ridge回归模型）所使用的具体GPU型号、数量或训练时长。由于这是一个规模极小的线性模型训练，可以推断其计算需求非常低。
*   在推理和应用部署中，论文提到了使用云服务 **Google Vertex AI** 来运行 **Qwen3-Embedding-8B** 模型，但未提供具体的算力配置或成本信息。

### 5. 实验数量与充分性
*   **实验数量**：主要实验在一个包含68个标注编辑对的单一数据集上进行。
*   **充分性与客观性**：
    *   **优点**：使用了留一交叉验证，在小样本情况下提供了较无偏的性能估计。与YARA和GPT-5.4两种基线方法进行了直接对比，定位了方法性能区间。
    *   **不足**：实验**不够充分**。
        *   仅验证了一个特质（数据寻求），缺乏在多个不同特质上的泛化能力验证。
        *   所有“之后”版本的技能文件都是合成和标注的，未使用真实世界中智能体自主演化产生的编辑数据，生态效度存疑。
        *   数据集规模较小（68对），且来源单一（仅一个仓库），存在数据集偏差和过拟合风险。
        *   未进行消融实验，例如测试不同嵌入模型、不同指令前缀或不同回归模型的影响。

### 6. 论文的主要结论与发现
*   提出的方法在识别“数据寻求”特质变化的方向上，取得了 **91.2%的符号分类准确率**和 **ρ=0.82的Spearman等级相关系数**，性能远超YARA规则基线（63.2%准确率）。
*   方法的主要优势在于其**确定性、速度快、可审计**。与GPT-5.4（100%准确率）相比，虽然准确性略低，但后者不可解释、开销大且结果不稳定。
*   在**6个误分类**的案例中，都发生在真实标签绝对值较小（变化微弱）的情况下，表明错误主要集中于难以区分的边界案例，而没有出现“确信的错误”。
*   本文成功将特质评估与一个**智能体间协议**相结合，通过一个去中心化的运行时服务器，实现了智能体间无需暴露原始文件即可进行自主评估，并提供了一个端到端的部署验证案例。

### 7. 优点
*   **方法创新性**：将源自表征工程（Representation Engineering）的“特质即方向”的思想，成功迁移到Agent的源文件分析场景，为监控自适应智能体的行为漂移提供了新颖且实用的思路。
*   **实用性与可审计性**：设计出了一种无需调用昂贵LLM、运行速度快、结果完全确定（对相同输入产生相同输出）的评分方法，非常适合需要高频、实时、可复现检查的生产环境。
*   **系统完整性**：不只停留在方法层面，还设计并演示了一个完整的“智能体间协议”（Agent-to-Agent Protocol），考虑了实际部署中的信任模型、安全威胁和通信流程，展示了从研究到落地的潜力。
*   **清晰的基线对比**：通过与传统规则方法（YARA）和前沿LLM（GPT-5.4）的对比，清晰地展示了本方法在性能、成本和解释性之间所处的权衡位置。

### 8. 不足与局限
*   **实验范围狭窄**：仅在一个特质、一个合成数据集上验证，严重限制了结论的普适性。真实世界中的技能文件编辑可能更复杂、更细微。
*   **数据基础薄弱**：68个样本的标注数据集太小，易受噪声影响，且无法进行更复杂模型的训练。合成数据的质量完全取决于标注者的判断，存在主观偏差。
*   **信任假设**：方法依赖于对嵌入模型和运行时服务器的信任，虽然协议设计试图减少对执行方（Agent B）的信任，但并未完全消除对中心化中介的依赖。
*   **未考虑对抗性编辑**：论文在影响声明中承认，该方法**可能被恶意行为者利用**，通过精心构造能规避检测的编辑来逃脱监控。论文未提出针对性的对抗性鲁棒性方案。
*   **缺乏深层次分析**：未分析特质向量的语义含义，也未探讨不同特质之间的关系。对于聚合多个技能分数的启发式方法，也仅仅是初步探讨，缺乏验证。

（完）
